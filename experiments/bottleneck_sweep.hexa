// bottleneck_sweep.hexa — Information Bottleneck Dimension Sweep (-> Law 95)
// Source: bottleneck_sweep.py
// Sweep dims: 128, 64, 32, 16, 8, 4, 2, 1

fn run_sweep(cells: int, steps: int) -> float {
    // TODO: measure Phi at each bottleneck dim
    0.0
}

fn main() {
    println("bottleneck_sweep: info bottleneck dimension sweep")
}
