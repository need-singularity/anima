// R37/AN13/L3-PY violation — python-in-hexa source removed 2026-04-19
// Original intent: TensionBridge 5-channel verification (WHAT/WHERE/WHY/
//                  TRUST/WHO) end-to-end — compress→weighted mix→expand→gate.
// Spec: anima.models.trinity.TensionBridge contract (T1~T5 tests).
//       Replacement must be hexa-native (no torch, no numpy fallback).

fn main() {
  eprintln("[R37/AN13/L3-PY] experiments/verify_tension_link.hexa: python-in-hexa source blocked.")
  eprintln("  Escalate to user for hexa-native reimplementation.")
  exit(1)
}
