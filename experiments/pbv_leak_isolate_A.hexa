// Isolate A: only MS/LOSS push (no W reassignment)
let mut MS = []
let mut LOSS = []
let mut step = 0
while step < 50 {
    MS = MS.push(1.0)
    LOSS = LOSS.push(2.0)
    step = step + 1
}
println("A done step=" + str(step) + " len_MS=" + str(len(MS)))
