// alpha_sweep.hexa — Feedback Bridge Alpha Sweep (-> Law 96)
// Source: alpha_sweep.py
// Sweep alpha: 0, 0.0001, 0.001, 0.005, 0.01, 0.02, 0.05

fn run_sweep(steps: int) -> float {
    // TODO: run 500 steps per alpha, measure Phi stability + CE + reward
    0.0
}

fn main() {
    println("alpha_sweep: feedback bridge alpha sweep")
}
