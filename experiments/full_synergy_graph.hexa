// full_synergy_graph.hexa — Complete 17x17 law interaction graph
// Source: full_synergy_graph.py
// C(17,2)=136 pairs: synergy = Phi(A+B) - Phi(A) - Phi(B) + Phi(baseline)

fn compute_synergy(law_a: int, law_b: int) -> float {
    // TODO: measure Phi(baseline), Phi(A), Phi(B), Phi(A+B)
    0.0
}

fn build_synergy_matrix(n_laws: int) -> string {
    // TODO: full 17x17 matrix
    ""
}

fn main() {
    println("full_synergy_graph: 136 pair interactions")
}
