// reward_feedback.hexa — Reward-Only Feedback Experiment (-> Law 100)
// Source: reward_feedback.py
// 3 conditions: full isolation, reward-only, gradient+reward

fn run_isolation(steps: int) -> float {
    // TODO: no feedback at all (.detach(), no reward)
    0.0
}

fn run_reward_only(steps: int) -> float {
    // TODO: .detach() + reward signal (1% perturbation)
    0.0
}

fn run_gradient_reward(steps: int, alpha: float) -> float {
    // TODO: alpha=0.01 + reward (known bad from Law 97)
    0.0
}

fn main() {
    println("reward_feedback: 3 feedback conditions")
}
