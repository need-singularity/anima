// run.hexa — Anima entry point (forwards to anima_unified via core/runtime/)
// Source: anima/run.py
// CLI: hexa anima/run.hexa

fn main() {
    // Path setup is handled by the hexa module system.
    // Delegates to core/runtime/anima_unified.hexa
    // TODO[hexa-import]: import core.runtime.anima_unified
    // anima_unified.main()
    print("Anima entry point — forwarding to core/runtime/anima_unified.hexa")
}
