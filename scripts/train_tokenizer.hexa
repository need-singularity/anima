// train_tokenizer.hexa — Tokenizer training with SentencePiece
// Ported from train_tokenizer.py (186 LOC)

fn train_tokenizer(input_path, model_prefix, vocab_size) {
    // TODO: train SentencePiece tokenizer on input corpus
    print("TODO: train_tokenizer")
}

fn verify_tokenizer(model_prefix, input_path) {
    // TODO: verify trained tokenizer on sample texts
    print("TODO: verify_tokenizer")
}

fn main() {
    // TODO: parse args, train tokenizer, run verification
    print("TODO: train_tokenizer main")
}
