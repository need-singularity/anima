// Ported from sync_readme.sh — Sync core_rules.json -> README.md (AUTO markers)
//
// R37/AN13/L3-PY (2026-04-18): original script embedded a python3 heredoc
// that used json.load + re.sub to patch AUTO:verification_status markers.
// Wrapper-fallback python3 call removed. See CLAUDE.md §.py 절대 금지.
// Hexa-native JSON -> README sync required (read core_rules.json in hexa,
// replace markers via string ops, write README.md).

fn main() {
    shell("echo '[R37/AN13/L3-PY] BLOCKED: scripts/sync_readme.hexa python3 json/re invocation removed 2026-04-18. Hexa-native README sync required. See CLAUDE.md.' 1>&2")
    exit(1)
}
