// train_supervised.hexa — STUBBED 2026-04-18 (R37/AN13/L3-PY compliance)
//
// Previously invoked python3 inside an immortal training wrapper (survives tmux
// death). Violates R37/AN13/L3-PY.

fn main() {
    println("[R37/AN13/L3-PY] BLOCKED: scripts/train_supervised.hexa")
    println("  Previously invoked python3 inside immortal training wrapper.")
    println("  Escalate to user for hexa-native replacement.")
    panic("R37/AN13/L3-PY: python3 invocation blocked at scripts/train_supervised.hexa")
}

main()
