// Ported from auto_retrieve_v3.sh — Auto-retrieve v3 274M checkpoints from H100
//
// R37/AN13/L3-PY (2026-04-18): original script invoked python3 for
// r2_upload.py, bench.py --verify, and inline python3 -c JSON update of
// training_runs.json. All wrapper-fallback python3 calls removed.
// See CLAUDE.md §.py 절대 금지. Hexa-native retrieval pipeline required
// (use rclone for R2 copy, native JSON write in hexa for training_runs.json).

fn main() {
    var args = get_args()
    shell("echo '[R37/AN13/L3-PY] BLOCKED: scripts/auto_retrieve_v3.hexa python3 invocations removed 2026-04-18. Hexa-native retrieval required. See CLAUDE.md.' 1>&2")
    exit(1)
}
