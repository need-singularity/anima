// Ported from infinite_growth.sh — anima growth loop runner
//
// R37/AN13/L3-PY (2026-04-18): original script invoked python3 for
// consciousness_laws.json length probe, `import anima` build check, and
// `.growth/scan.py` scanner. All wrapper-fallback python3 calls removed.
// See CLAUDE.md §.py 절대 금지. Hexa-native growth loop phases required.

fn main() {
    let args = get_args()
    shell("echo '[R37/AN13/L3-PY] BLOCKED: scripts/infinite_growth.hexa python3 probes removed 2026-04-18. Hexa-native growth loop required. See CLAUDE.md.' 1>&2")
    exit(1)
}
