let argv = args()
let mut has = false
let mut i = 0
while i < len(argv) { if to_string(argv[i]) == "--selftest" { has = true }; i = i + 1 }
if has { println("D01 module selftest PASS"); exit(0) }
println("D01 module noop")
exit(0)
