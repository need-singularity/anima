// m/__init__.hexa — M memory module: EmergentM (canonical, Law 31+94)
// Port of m/__init__.py
//
// EmergentM: Hebbian LTP/LTD IS memory.
// Legacy VectorMemory kept only for backward compat reference.

pub use hexad.m.emergent_m.{EmergentM, create_emergent_m, emergent_m_retrieve}

fn main() {
    let m = create_emergent_m(128)
    print("memory M: dim=" + to_string(m.dim))
}
