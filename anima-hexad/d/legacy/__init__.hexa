// d/legacy/__init__.hexa — LEGACY D decoders
// Port of d/legacy/__init__.py
//
// WARNING: Legacy decoders are deprecated. Use ConsciousDecoderV2 instead.
// TransformerDecoder, MLPDecoder, HFDecoder, ConsciousLM kept for reference only.

// TODO[runtime]: pub use trinity.{TransformerDecoder, MLPDecoder, HFDecoder}
// TODO[runtime]: pub use conscious_lm.ConsciousLM

struct TransformerDecoder {
    d_model: int,
    vocab_size: int
}

struct MLPDecoder {
    d_model: int,
    vocab_size: int
}

struct HFDecoder {
    d_model: int,
    vocab_size: int
}

struct ConsciousLM {
    d_model: int,
    vocab_size: int
}

fn main() {
    print("WARNING: legacy D decoders deprecated — use ConsciousDecoderV2")
}
