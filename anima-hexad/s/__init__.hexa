// s/__init__.hexa — S sense module: EmergentS (canonical, Law 4)
// Port of s/__init__.py
//
// EmergentS: consciousness state delta IS perception.
// Legacy TensionSense kept only for backward compat reference.

pub use hexad.s.emergent_s.{EmergentS, create_emergent_s, emergent_s_process}

fn main() {
    let s = create_emergent_s(128)
    print("sense S: dim=" + to_string(s.dim))
}
