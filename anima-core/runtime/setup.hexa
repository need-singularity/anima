// setup.hexa — Anima initial setup (~/.anima/ structure)
// TODO[pytorch]: port directory creation, R2 config, model download
//
// ⚠ DEPRECATED 2026-04-11 — absorbed into runtime_actions.hexa
//   canonical: anima/core/runtime/runtime_actions.hexa  rt_setup_*
//   dispatcher: run_action("setup", "")
//   commit: anima 8008d0fb (consolidation)

fn setup_minimal() -> bool {
    // TODO[pytorch]: create ~/.anima/ directory tree
    false
}

fn setup_download_model() -> bool {
    // TODO[pytorch]: download checkpoint from R2 to ~/.anima/models/
    false
}

fn setup_status() -> string {
    // TODO[pytorch]: report current setup state
    ""
}

fn setup_interactive() -> int {
    // TODO[pytorch]: guided setup wizard
    0
}
