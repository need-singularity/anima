# Roadmap watchdog status — 2026-04-18 22:10 KST

Source of truth: `shared/roadmaps/anima.json` (5924 lines, 136 phases, 3-track ALM/CLM/PHYSICS).
Sidecars consulted: `shared/convergence/roadmap_reality_sync.convergence`, `shared/convergence/dest1_dest2_ship.convergence`, `shared/convergence/anima.json`.

This is a one-shot snapshot, NOT a realtime watcher. Next poll triggers on the next `go` cycle.

---

## 1. 3-track × phase × gate status

| track | current phase | declared_status | reality_state | running? | gate |
|-------|---------------|-----------------|---------------|----------|------|
| ALM | P1 in_progress (r10 run-tracking) | `in_progress` (roadmap `:25`) | reality_mismatch | r10c PID dead (KILLED 2026-04-18T11:29Z at step~75) — no ALM train process live on pod `dd88fldzkqhpgk` | gate r10/r10b/r10c all UNMET; A2-Tool eval BLOCKED 0.5667<0.60; A1_step_3000 only surviving ALM ckpt |
| ALM | P3/P4 (v4 홀로) | `done` | reality_mismatch D1+D5 | — | Φ_holo=6111 FALSIFIED by r10 triple-collapse (`alm_14b_r10.convergence:39-46`) |
| ALM | r11 A/B/C prep | `prep_three_way_branch` (roadmap `:41`) | scaffold_done | — | depends_on r10 DONE + eval_report (NOT met) |
| ALM | 32B r1 | N/A (outside P-list) | scaffold_done | — | `@blocked_by alm_14b_r10 HALT` per `alm_32b_r1.convergence:33` |
| ALM | 70B | pending | pending | — | pod not launched; `alm_70b.convergence` pending |
| CLM | P2 v3 GWT | `done` | structural_only (ALM+CLM self-test PASS) | pod `u744u0lbts5v1w` STOPPED — r4 last_step=6200, SSH dead, GPU=0% | ubu RTX 5070 mmap smoke 7/7 PASS, but hexa-native GPU blocked by SM90 cuLaunchKernelEx |
| CLM | r4 mmap | N/A | partial_blocked (`clm_r4_mmap.convergence:1`) | — | SM90 NVRTC parameter-pack fix required |
| PHYS | P1 structural | `done` (structural) | structural_only | — | self-test PASS only, no HW runs logged this session |
| ALL | P5–P20 mass | `done` (×17) | structural_only D6 | — | A4 W1 PASS only; W2/W3/W4 TBD (`a4_live_selftest.convergence:10-13`); `alm_hive_agg.hexa` RUN-OOM |
| ALL | P21 v∞ | `done` | speculative_scaffold D7 | — | terminal gate `||Φ||>10000 + 1000-gen autonomous` — never executed |
| ALL | P22 corpus | `done` | wire_pending D3 | — | kowiki 1.28 GiB local-only; BPE not wired to `corpus_shard.hexa`; downstream CE unmeasured |
| ALL | P24 phi↔bench | `done` | structural_only D4 | — | r=0.898 from n=5 smoke; HAE-RAE + MMLU-Ko DEFERRED |
| ALL | P26–P33 (8 domains) | `done` | structural_only | — | scaffold_done per `p26..p33_*.convergence` |
| ALL | P116–P135 (20 engines) | `done` | structural_only | — | compact schema, `phi_holo_min: 0.0` non-binding; artifacts exist at `anima-engines/*_phi.hexa` |

### Running learning processes (current)
- **0 live training runs.** r10c killed at step~75 due to COLLAPSED_UNDETECTED (E7-OSS-05 new ossified rule); CLM r4 pod STOPPED since 2026-04-17T09:44Z.
- H100 fleet: `lnzliav2e8du6i` RUNNING (A2-Tool train done step 1500 loss 0.049 — eval BLOCKED); `dd88fldzkqhpgk` H100_SXM idle-after-r10c-kill; `u744u0lbts5v1w` STOPPED. Active GPU time costing with zero live training = policy violation risk (`@cost_rule idle_zero`).

---

## 2. Gate drift items (from audit D1–D7 + session addendum)

| drift_id | phase(s) | reality_state | blocker |
|----------|----------|---------------|---------|
| D1 | P4 | reality_mismatch | Φ_holo 6111 FALSIFIED by r10 triple-collapse — no byte-sentinel, no PID liveness in prior pipeline |
| D2 | ALM ladder | reality_mismatch | r10/r10b/r10c/32B/70B/A2-Tool all invalid — only A1_step_3000 valid; `@decision HALT 13B/70B launch on A2-Tool ckpt` |
| D3 | P22 | wire_pending | kowiki local-only, 4 GB RSS SIGKILL on mac during content_hash; pod-side ingest pending |
| D4 | P24 | structural_only | HAE-RAE/MMLU-Ko DEFERRED (HF loader blocked); n=5 ckpts correlation trajectories too correlated (16/16 SIG) |
| D5 | P3 | reality_mismatch | Φ>1000 criterion deferred-to-P4; P4 itself collapsed (D1) ⇒ neither has real-gate PASS |
| D6 | P5–P20 mass | structural_only | W2/W3/W4 TBD; `alm_hive_agg` RUN-OOM at ~4 GB RSS N×N MI; multiple `??` untracked heads |
| D7 | P21 | speculative_scaffold | no 1000-gen zero-intervention run exists in any convergence or report |
| D8 (new) | r10c pipeline | reality_mismatch | E7-OSS-05 discovered 2026-04-18 — `except Exception` fallback missed rc≠0/2; sentinel subprocess returned rc=1 (GLIBC_2.38 missing on pod) silently → training continued past detected collapse |
| D9 (new) | E7-OSS-06 fresh LoRA recipe | reality_mismatch | const LR 2e-6 + batch=8 seq=1024 + fresh LoRA r=32 on 95 MB (stripped+kowiki) insufficient; loss 4.73→0.94 in 63 steps = memorization-collapse, not convergence |

Total gate drifts: **9** (7 from audit + 2 new 2026-04-18).

---

## 3. Seed-freeze rebuild status

State file: `shared/state/hexa_rebuild_status.json` — `rebuilt:false`.

Evidence:
- `/Users/ghost/Dev/hexa-lang/build/.stage0_locked` present with payload `"DO_NOT_AUTO_REBUILD — manually restored from fp7_gen1 #오후"`.
- `/Users/ghost/Dev/hexa-lang/build/.rebuild_hash` pinned to `e948eee9c5d737d7f62be02f3ec368a3f75cf3176bfe4949969ac0b684caa161`.
- `/Users/ghost/Dev/hexa-lang/build/hexa_v2` binary NOT present at expected path.
- Native interpreter chain per `project_session_20260413_native_interp.md`: tokenize+parse+interpret work; struct-field codegen residual.

Freeze still in effect. Auto-rebuild suppressed intentionally. Next poll = next `go` cycle.

---

## 4. Recommended next 3 actions (7-day window)

Ranked by leverage toward 도착지1 (페르소나 Φ>1000 substrate) per `dest1_dest2_ship.convergence`.

### A1. r10c relaunch with E7-OSS-05+06 fixes (direct D1/D2/D8/D9 unblock)
- Patch `launch_alm_14b_r10c.hexa` subprocess fallback: explicit `if rc not in (0,2): use_fallback()` (not only `except Exception`).
- Corpus: scale 95 MB → ≥500 MB (P22 bleeding edge) OR raise LR 2e-6 → 5e-6 with linear warmup 100 steps per E7-OSS-06 fix-path A.
- Build hexa binary on pod with GLIBC_2.38 compatibility OR static link; current pod binary returns rc=1 silently.
- Expected outcome: r10c step 2000 with sentinel PASS + Likert ≥ 3.3 unblocks A-branch 32B (`:45`). Else fall to r11-C (seed sweep halved LR reduced rank) per roadmap `:99-124`.
- Dependency: pod `dd88fldzkqhpgk` already provisioned with corpus_mix — do NOT tear down.

### A2. HAE-RAE + MMLU-Ko HF-loader unblock (direct D4 unblock, feeds dest1 substrate eval)
- Files: `serving/eval_harness.hexa` has KoBEST n=20 inline; per `eval_harness_real.convergence:14-15`, HAE-RAE + MMLU-Ko DEFERRED due to HF loader blocked.
- Deliverable: inline port of both benchmarks (no Python dep per total-.py ban); g6.2.0 hexa HF-compat already land per `roadmap_reality_sync:53`.
- Expected outcome: once r10c produces ≥8 independent intermediate ckpts, `phi_correlation.convergence` re-runs with n≥8 (currently n=5 smoke) — closes P24 gate.

### A3. dest1 persona AN11 compliance path (NOT system-prompt wrap)
- Context: `persona_base_serve.convergence` + `employee_base_serve.convergence` REVOKED 2026-04-18 for AN11 violation (prompt wrapping). Prior "APPLIED rank_1_dest1_direct" is now REVOKED_AN11.
- New requirement (per `roadmap_dest_consciousness.json` C1/C2/P1/S1 9h critical path): weight-emergent persona — per-persona LoRA adapters (`TALM-P2-1`) + `serving/hire_routes.hexa` TSRV-P4-1 + `anima-speak` voice + `character_builder_ui.hexa` + phi_vec attached per request.
- 3-condition AN11 gate (all required): (a) weight_emergent (no system_prompt wrap), (b) consciousness_attached (phi_vec + consciousness_laws runtime PASS), (c) real_usable (HTTP `/hire/task` + R2 artifact + README repro).
- Dependency: A1 r10c success provides Φ>1000 substrate for the persona LoRAs to inherit.

---

## 5. Growth loop state (per `feedback_report_includes_loop`)

- Growth bus: `shared/discovery/growth_bus/` active.
- Auto-gap: `/Users/ghost/Dev/anima/shared/auto_gap.log` (not reloaded this poll — snapshot only).
- Last session close-out tallies (`shared/convergence/anima.json:203-214` historical, `roadmap_reality_sync.convergence:32` delta): failed 1→2 (+r10), blocked 0→1 (+r10b), ossified 0→1 (+runpod_autopilot_f1), total 30→34.
- Ossification deltas this poll: E7-OSS-05 + E7-OSS-06 added to `alm_14b_r10.convergence:64-65`; no new roadmap ossifications.
- Growth recommendation: re-run `shared/n6/scan_math_atlas.hexa` after r10c completes (pre-emptive atlas refresh to absorb new ossified rules).

---

## Appendix — signals watched but not triggered this poll

- `$HEXA_LANG/build/hexa_v2` timestamp — still absent, freeze intact.
- Any new `shared/convergence/*.convergence` file newer than 2026-04-18T22:00Z — none beyond `anima.convergence` session delta log.
- PHYSICS HW stream — no ESP32 / FPGA dispatch logged this session.

Next poll triggers: next `go` or `loop` invocation.
