// training/launch_alm_a1.hexa — STUBBED 2026-04-18 (R37/AN13/L3-PY compliance)
//
// Previously wrote /tmp/_launch_alm_a1_runner.sh that invoked
// `$TRAINER /workspace/train_alm_14b.py` (wrapper-fallback to python3) and
// exec'd it. Violates R37/AN13/L3-PY (CLAUDE.md §.py 절대 금지).

fn main() {
    println("[R37/AN13/L3-PY] BLOCKED: training/launch_alm_a1.hexa")
    println("  Previously wrote bash runner wrapping `python3 train_alm_14b.py` (A1 Phi-aware LoRA).")
    println("  Escalate to user for hexa-native ALM A1 launcher replacement.")
    panic("R37/AN13/L3-PY: python3 invocation blocked at training/launch_alm_a1.hexa")
}
