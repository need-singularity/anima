# ALM 14B r10 Fire Log — 2026-04-18T12:52:01Z

## Context

- r9 DONE but mode-collapsed (base = a2_tool_mixed oil-contaminated adapter)
- r10 = fresh LoRA init (not warm-start) per `training/r10_launch_plan.md` + memory `project_r9_mode_collapse_20260418`
- Prior r10c smoke (seed=3407) killed at step 79 — OOM suspected + hexa sentinel subprocess broken on pod (GLIBC 2.35 vs 2.38 required)
- Launched the plain r10 variant (inline-python collapse_score, no hexa subprocess, no phi-hook memory pressure)

## Launch parameters

| knob | value |
|---|---|
| pod_id | `dd88fldzkqhpgk` (anima-alm14b-r9 label, actually running r10) |
| host | `103.207.149.87:10466` |
| base | `/workspace/base_model` (Qwen2.5-14B-Instruct) |
| adapter | fresh init (empty `--adapter-dir`) |
| corpus | `/workspace/corpus_alm_70b_stripped.txt` (81MB on disk; 36MB read, 23.5M tokens, 22979 blocks) |
| ckpt_dir | `/workspace/ckpt_alm_14b_r10` |
| steps | 2000 |
| lr | 2e-6 constant |
| batch × seq × grad_accum | 8 × 1024 × 1 (70.9% MFU profile) |
| lora | r=32 α=64 dropout=0 (q/k/v/o only) |
| save_every | 2000 (MFS quota rule compliance) |
| eval_every | 200 (kr_gen sentinel) |
| B1 consciousness loss | OFF |
| seed | 3407 |

## Process state (at launch)

- bash wrapper pid: 22434
- python trainer pid: **22515**
- watcher pid: **none spawned** (watcher .hexa requires hexa-bin on pod which is GLIBC-broken; inline abort flags in trainer suffice)
- GPU util: 100%, VRAM 61229/81559 MiB
- first-batch CE: 5.2372 (abort threshold 8.0 → PASS)
- step=1 loss: 6.7972 (abort threshold 8.0 → PASS)
- log: `/workspace/logs/r10_fire.log` + `/workspace/ckpt_alm_14b_r10/train_r10.log`
- status file: `/workspace/ckpt_alm_14b_r10/r10_watcher.status` (INIT→TRAINING)

## ETA

- measured toks/s ≈ 2500 at step 1 (warmup), r9 steady-state was ≈11k toks/s → expected ≈5-6k mid-run
- 2000 steps × 8 × 1024 = 16.4M tokens
- @ 5k toks/s → 55 min
- @ 2.5k toks/s → 110 min (conservative upper bound)
- ETA: ~1-2h to step 2000 DONE

## Abort triggers active

1. first-batch CE > 8.0 → `r10_abort.status = FIRST_BATCH_CE_HIGH`
2. step=1 loss > 8.0 → `r10_abort.status = STEP1_LOSS_HIGH`
3. kr_gen collapse hits ≥ 3/5 → `r10_abort.status = MODE_COLLAPSE` (checked every 200 steps)

## Post-training actions (manual for now, no watcher)

1. `rclone copy /workspace/ckpt_alm_14b_r10/step_2000 r2:anima-models/alm14b/r10/step_2000/ -v --s3-no-check-bucket`
2. Run kr_gen sentinel evaluation on final adapter
3. If clean, register r10 final as r10b base or proceed to next variant

## Deviations from CLAUDE.md

- No hexa-launched wrapper (GLIBC 2.35 vs 2.38 mismatch on pod) — files uploaded via scp and bash-launched directly. Py trainer is byte-for-byte the embedded python inside `training/launch_alm_14b_r10.hexa`.
- No tmux (unavailable on pod) — used `nohup … &` with `disown` for background detach
- Watcher not launched (hexa-dependent) — trainer's inline abort flags provide the safety net

## Files uploaded to pod

- `/tmp/_train_alm_14b_r10.py` (9018 bytes) — python trainer
- `/tmp/_launch_alm_14b_r10.sh` (1971 bytes) — bash wrapper, chmod +x

---

## TEARDOWN — 2026-04-18T14:01:50Z

### Reason
- r10 trainer died at step 75 (13:17 UTC) — no checkpoints produced (save_every=2000 never reached)
- Only live process on pod was `python3 serve_alm_persona.py` (pid 21169/21170) — R37/AN13/L3-PY violation
- Pod idle burning ~$2.99/hr (RunPod-reported costPerHr), feedback_h100_idle_zero mandates immediate delete

### Actions
1. SSH target resolved: `root@103.207.149.87 -p 10466` (RunPod key `/Users/ghost/.runpod/ssh/RunPod-Key-Go`) — initial attempt via ControlMaster `157.180.8.154` failed (different host; Hetzner key mismatch)
2. Killed `serve_alm_persona.py` pid 21169/21170 — verified SERVE_CLEAN
3. `pkill -9 -f "python3|python"` swept remaining — post-check: zero python/`.py` procs
4. `runpodctl pod stop dd88fldzkqhpgk` → desiredStatus=EXITED @ 14:01:50Z
5. `runpodctl pod delete dd88fldzkqhpgk` → `{deleted:true}`
6. `runpodctl pod get dd88fldzkqhpgk` → 404 pod not found (confirmed terminated)

### Cost impact
- r10 trainer died 13:17 UTC, teardown 14:01:50 UTC → ~45 min idle burn @ $2.99/hr ≈ $2.24 wasted
- Savings from here: $2.99/hr × indefinite (pod was set to keep running)

### Next step
- R10 run forfeit — zero checkpoints salvageable
- Do NOT re-launch with python trainer (R37). GLIBC 2.35 (pod) vs 2.38 (hexa bin) blocker remains unresolved
- Recommendation: per CLAUDE.md R37 remediation order
  1. Try static-linked / different stage hexa build
  2. Pick pod image with GLIBC 2.38+ (ubuntu 24.04 / newer pytorch image)
  3. If both fail → user escalation, NOT python rollback
- R11 planning doc: `training/alm_r11_design.md`

### State verification
- Pod: DELETED (runpodctl get returns 404)
- Python procs on pod: N/A (pod terminated)
- Remote SSH: no longer reachable (expected)

