# ALM r10 — eval plan (post-training)

**Trigger**: training/r10_train.log reports `[r10] DONE.` on pod l9mri0x8sqpmbb.

## Pre-Z context (still relevant)

Z lenient judge already pushed r9 from 0.5333→0.867 with no retraining
(commit ce4dbce2, 2026-04-16). r10 is no longer the rubric-driven priority
it was when designed.

What r10 still buys:
- Shorter responses (speed)
- Tighter completion on the 4 remaining XX domains under lenient judge
  (email/easy 3/5, meeting/easy 4/5, doc/easy 4/5)
- Longer-term: saves $ on Path C augmentation if r10 internalizes the
  format mapping

## Step 1 — confirm training completed cleanly

```bash
ssh -i /Users/ghost/.runpod/ssh/RunPod-Key-Go -p 13559 \
  root@103.207.149.92 \
  "tail -30 /workspace/r10_train.log; ls /workspace/ckpt_alm_14b_r10/final/"
```

Expect: `[r10] DONE. total_sec=...` line + `final/adapter_model.safetensors`.

## Step 2 — download r10 ckpt to Mac (R2 backup)

```bash
mkdir -p /tmp/r10_ckpt && \
scp -i /Users/ghost/.runpod/ssh/RunPod-Key-Go -P 13559 -r \
  root@103.207.149.92:/workspace/ckpt_alm_14b_r10/final/ \
  /tmp/r10_ckpt/

# R2 backup (memory rule: r2_checkpoint_backup)
rclone copy /tmp/r10_ckpt/final r2:anima-models/alm14b/r10/final/ -P
```

## Step 3 — launch r10 inference server on r10 pod

```bash
ssh -i /Users/ghost/.runpod/ssh/RunPod-Key-Go -p 13559 \
  root@103.207.149.92 \
  "cd /workspace && nohup hexa anima/serving/serve_alm_14b.hexa \
     --ckpt /workspace/ckpt_alm_14b_r10/final \
     --port 8091 \
     > /workspace/r10_serve.log 2>&1 < /dev/null & disown"
```

Pod has only 22/tcp exposed — use SSH local-forward from Mac:

```bash
ssh -i /Users/ghost/.runpod/ssh/RunPod-Key-Go -p 13559 \
  -L 8091:localhost:8091 -N \
  root@103.207.149.92 &
```

## Step 4 — hire_sim eval against r10 (BOTH judges)

```bash
# strict (compare apples-to-apples vs r9 0.5333)
HIRE_SIM_ENDPOINT=http://localhost:8091/generate \
  hexa training/deploy/hire_sim_baseline.hexa \
    --tasks 30 --out training/deploy/hire_sim_alm_r10_strict_20260416.json

# lenient (compare to r9 0.867 ceiling)
HIRE_SIM_ENDPOINT=http://localhost:8091/generate \
  hexa training/deploy/hire_sim_lenient.hexa \
    --tasks 30 --out training/deploy/hire_sim_alm_r10_lenient_20260416.json
```

## Step 5 — decide r11

| r10 strict | r10 lenient | Verdict |
|---|---|---|
| > 0.75 | > 0.90 | **WIN** — r10 internalized the format mapping. Propose r11 with mixed corpus (synth + B-agent's real corpus subset). |
| 0.55–0.75 | 0.85–0.90 | partial — r10 helped but not transformative. Investigate failure modes per domain. |
| ≤ 0.55 | < 0.85 | **NO-OP** — synth corpus too small/templated. Pivot to TALM-P4-1 (657b23bd) real corpus path, $245/4-6일. |

## Step 6 — cleanup

```bash
runpodctl pod stop l9mri0x8sqpmbb   # save $2.99/h
# r9 pod 0sc9odwk577j0y already stopped
# ALM live pod itfl66q4z768kh stays (LIVE)
```

Total expected eval cost: ~$1 (10 min serve + 5 min eval on r10 pod).
