# CLM r4 pod3 Fire Log — 20260418T150931Z

## Timeline
- **Creation start**: 2026-04-18T15:09:31Z
- **Creation end**:   2026-04-18T15:09:32Z  (pod API returned RUNNING desired)
- **SSH ready**:      ~3s after SSH endpoint exposed (IP+port appeared ~60-70s post-create)
- **Pre-flight done**: 2026-04-18T15:17:xxZ

## Pod
- **pod_id**: `pvref8zw70kv1z`
- **name**: `anima-clm1b-r4-pod3`
- **GPU**: NVIDIA H100 80GB HBM3 (H100 SXM, US datacenter)
- **vCPU / RAM**: 26 vCPU / 251 GB
- **Disk**: 100 GB container + 100 GB /workspace volume
- **SECURE cloud**
- **Cost**: **$2.99/hr** ($71.76/day — per H100 zero-idle policy)
- **Image**: `runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04`

## SSH Endpoint
- **ip**: `64.247.201.53`
- **port**: `10051`
- **key**: `/Users/ghost/.runpod/ssh/RunPod-Key-Go`
- **connect**: `ssh -i /Users/ghost/.runpod/ssh/RunPod-Key-Go root@64.247.201.53 -p 10051`

## Create Command
```
runpodctl pod create \
  --name anima-clm1b-r4-pod3 \
  --image runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04 \
  --gpu-id "NVIDIA H100 80GB HBM3" \
  --gpu-count 1 \
  --cloud-type SECURE \
  --container-disk-in-gb 100 \
  --volume-in-gb 100 \
  --volume-mount-path /workspace \
  --ports "22/tcp" \
  --ssh \
  --env '{"PYTHONUNBUFFERED":"1","PYTORCH_CUDA_ALLOC_CONF":"expandable_segments:True","ANIMA_ROOT":"/workspace/anima","HF_HOME":"/workspace/hf_cache"}'
```

## Pre-flight Results
| check | result |
|-------|--------|
| /workspace mount | MFS `us-mo-1.runpod.net:9421`, 378T total, 185T avail — OK |
| rclone | `/usr/bin/rclone` — OK |
| tmux | missing on boot; `apt-get update && apt-get install -y tmux` -> `/usr/bin/tmux` (3.2a-4) — OK |
| MFS quota write (20 GB dd) | MFS_OK — no hidden quota trap on this session |
| GPU | `GPU 0: NVIDIA H100 80GB HBM3` — OK |

## Environment Injected
- `PYTHONUNBUFFERED=1`
- `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`
- `ANIMA_ROOT=/workspace/anima`
- `HF_HOME=/workspace/hf_cache`

Note: PYTHONUNBUFFERED/PYTORCH_CUDA_ALLOC_CONF are carried from prior runpod template convention; .py execution remains forbidden per L3-PY. All training workloads must be hexa binaries.

## Next Steps Checklist
- [ ] Deploy hexa trainer binary to `/workspace/anima/training/` (scp hexa binary, NOT .py)
- [ ] Stage dataset + tokenizer to `/workspace/hf_cache`
- [ ] Pre-allocate & rotate old ckpts before run (MFS quota defense — ref: feedback_runpod_mfs_quota)
- [ ] tmux session `clm1b-r4` with trainer launcher
- [ ] Live loss + phi gate validation (ref: feedback_gate_vs_live_drift — don't trust PASS without serve validation)
- [ ] R2 upload deferred until post-training

## Constraints Honored
- `runpodctl pod create` invoked exactly 1x (succeeded; no retries)
- No .py created/edited/executed
- No pod deletion attempted
- No commits made (Do NOT commit)
