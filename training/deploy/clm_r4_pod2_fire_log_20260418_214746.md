# CLM r4 Pod 2 — FIRE LOG

**Status:** FIRED
**Fire timestamp (local):** 2026-04-18 21:47:46 KST
**Fire timestamp (UTC):** 2026-04-18 12:46:03 UTC (per `runpodctl pod get.createdAt`)
**Authorization:** `feedback_h100_authorized` (max 2 H100 pods, no confirmation required)

## Pod identity

| Field | Value |
|-------|-------|
| pod_id | `16oxzm6py5xdss` |
| name | `anima-clm1b-r4-pod2` |
| gpuType | NVIDIA H100 80GB HBM3 |
| gpuCount | 1 |
| vcpuCount | 26 (RunPod allocated, we requested 16 min) |
| memory | 251 GB (RunPod allocated, we requested 64 min) |
| containerDisk | 100 GB |
| volume | 0 GB (ephemeral — R2 watcher mandatory) |
| image | runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04 |
| secureCloud | true |
| ssh_ip | 216.243.220.224 |
| ssh_port | 10704 |
| ssh_key | `/Users/ghost/.runpod/ssh/RunPod-Key-Go` |
| cost | $2.99 / hr |

## Both pods running (2/2 H100 cap respected)

| Pod | Workload | Status |
|-----|----------|--------|
| `dd88fldzkqhpgk` | ALM r9 14B | RUNNING |
| `16oxzm6py5xdss` | CLM r4 1B | RUNNING (this fire) |

## Fire command executed

```
runpodctl create pod \
  --name anima-clm1b-r4-pod2 \
  --imageName runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04 \
  --gpuType 'NVIDIA H100 80GB HBM3' \
  --gpuCount 1 --containerDiskSize 100 --volumeSize 0 \
  --mem 64 --vcpu 16 --ports '22/tcp' \
  --startSSH --secureCloud \
  --env PYTHONUNBUFFERED=1 \
  --env PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
  --env CUBLAS_WORKSPACE_CONFIG=:4096:8 \
  --env ANIMA_ROOT=/workspace/anima \
  --env HF_HOME=/workspace/hf_cache \
  --env TRANSFORMERS_CACHE=/workspace/hf_cache
```

Note: spec file `training/deploy/clm_r4_runpod_create.json` uses schema
keys (`gpuTypeId`, `minMemoryInGb` etc.) not accepted by installed
`runpodctl 2.1.9` CLI flags. Used Method B (CLI flags) that the plan
explicitly pre-authorized. Spec file retained for reference/future pod
API.

## Cost ETA

| Scenario | Wall (h) | Total |
|----------|----------|-------|
| R2-resume (5K → 750K) **chosen** | 43.3 | **$129.5** |
| Fresh (0 → 750K) | 44.0 | $131.6 |
| Option C fallback (1.5M steps) | 82.1 | $245.5 |

Expected completion: **~2026-04-20 08:00 UTC** (43.3h from 12:46 UTC fire).

## Teardown criteria (all-of, per `feedback_pod2_delete_after`)

1. `/workspace/ckpt_clm1b_r4/training_summary.json` present
2. `rclone ls r2:anima-models/clm1b/r4/step_750000/` non-empty
3. eval_loss ≤ 1.0 recorded
4. → `runpodctl pod delete 16oxzm6py5xdss`

## Abort criteria (any triggers stop)

- MFS quota dd preflight fails → terminate, rebuild
- First 100 steps: loss NaN/Inf or not decreasing → abort
- Step 50000 eval > 2.0 → stop, re-evaluate corpus
- Wall clock > 60h with eval > 1.1 → stop, save best, option C next round
- Pod unresponsive (R12) → sync latest ckpt to R2, terminate, rebuild

## Watcher state (shared/config/r2_sync_watchers.json)

- `clm1b_r4_pod2` entry updated: `enabled=true`, `pod_id=16oxzm6py5xdss`,
  `host=root@216.243.220.224`, `port=10704`,
  `ssh_opts` includes `-i /Users/ghost/.runpod/ssh/RunPod-Key-Go`.
- launchd `com.anima.r2-sync-watcher.plist` picks up every 2 h (per
  `shared/config/r2_sync_watchers.json._meta.schedule`).
- First sync is a no-op until bootstrap writes the first ckpt (from
  R2-resume at step 5000 immediately after `clm_r4_launch.hexa` start).

## Bootstrap (next-steps, decoupled from this fire)

Bootstrap script runs in background (separate process from this fire).
Sequence mirrors `training/deploy/clm_r4_pod2_launch.md#Bootstrap`:

1. `ssh root@216.243.220.224 -p 10704 'mkdir -p /workspace/anima/training'`
2. `scp training/train_clm.hexa + training/clm_1b_config.json + training/deploy/clm_r4_launch.hexa`
3. `ssh ... 'rclone copy r2:anima-models/clm1b/r4/step_5000.hexackpt /workspace/ckpt_clm1b_r4/ -v'`
4. hexa binary install (pre-built H100 or bash fallback)
5. `ssh ... 'tmux new -d -s clm-r4 "hexa /workspace/clm_r4_launch.hexa 2>&1 | tee /workspace/clm_r4_launch.log"'`

Bootstrap may require manual verification of sshd readiness (~1-3 min
post-fire). Not blocking for this fire log — watcher handles ckpt
persistence independently once first write lands.
