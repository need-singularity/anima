// training/launch_alm_a2_tool_mixed.hexa — STUBBED 2026-04-18 (R37/AN13/L3-PY compliance)
//
// Previously wrote /tmp/_launch_a2_tool_mixed_runner.py (!!!) + a bash wrapper
// that executed `python3 -u launch_a2_tool_mixed.py`. This is a DOUBLE
// violation of R37/AN13/L3-PY — both write_file(".py") and python3 invocation.
//
// The prior comment claiming "write_file() → exec python3 is documented escape
// hatch" is NOT valid policy. Per CLAUDE.md §.py 절대 금지 (2026-04-18):
// "create: *.py 파일 작성/복사/다운로드 금지" — no runtime emission allowed.

fn main() {
    println("[R37/AN13/L3-PY] BLOCKED: training/launch_alm_a2_tool_mixed.hexa")
    println("  Previously wrote /tmp/_launch_a2_tool_mixed_runner.py AND invoked python3.")
    println("  Both operations forbidden. Escalate to user for hexa-native replacement.")
    panic("R37/AN13/L3-PY: python3 invocation + write_file(\".py\") blocked at training/launch_alm_a2_tool_mixed.hexa")
}
