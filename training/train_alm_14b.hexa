// train_alm_14b.hexa — STUBBED 2026-04-18 (R37/AN13/L3-PY compliance)
//
// Previously wrote /tmp/_train_alm_14b_runner.py at runtime (~1400 lines of
// embedded Python source) and exec'd `python3 /tmp/_train_alm_14b_runner.py`.
// Both the write_file(".py", ...) and python3 invocation violate R37/AN13/L3-PY
// (shared/rules/common.json#R37, shared/rules/anima.json#AN13,
//  shared/lockdown/lockdown.json#L3-PY, CLAUDE.md §.py 절대 금지).
//
// No hexa-native ALM 14B trainer exists. This launcher is intentionally
// inoperable until a hexa-native replacement lands. Escalate to user.

fn main() {
    println("[R37/AN13/L3-PY] BLOCKED: training/train_alm_14b.hexa")
    println("  This launcher previously generated /tmp/_train_alm_14b_runner.py")
    println("  and invoked python3 — both forbidden under CLAUDE.md §.py 절대 금지.")
    println("  Escalate to user for hexa-native ALM 14B trainer replacement.")
    panic("R37/AN13/L3-PY: python3 invocation + write_file(\".py\") blocked at training/train_alm_14b.hexa")
}
