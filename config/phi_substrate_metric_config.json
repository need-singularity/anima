{
  "schema": "anima/phi_substrate_metric/1",
  "version": "2",
  "generated": "2026-04-23",
  "purpose": "Invariant Φ cross-path metric config — gram eigenvalue spectrum + participation ratio + null-model bootstrap threshold (roadmap #90)",
  "spec_doc": "docs/phi_substrate_metric_spec.md",

  "prompts": [
    "The substrate of consciousness is",
    "When an agent recognizes itself, it",
    "Information integration across modules yields",
    "Phi measures the",
    "A self-model distinguishes agent from",
    "The essence of experience lies in",
    "Recurrent binding of features produces",
    "Intrinsic existence requires",
    "Cause-effect power is measured by",
    "A complex of consciousness is",
    "Differentiation and integration together",
    "The maximum of phi indicates",
    "Unfolded qualia space contains",
    "Exclusion means consciousness is",
    "Composition in IIT refers to",
    "The minimum information partition"
  ],
  "prompt_count": 16,

  "paths": {
    "p1": {
      "model": "Qwen/Qwen3-8B",
      "arch_family": "qwen3",
      "param_b": 8.2,
      "expected_hidden_dim": 4096,
      "auth_required": false,
      "dtype": "bf16"
    },
    "p2": {
      "model": "mistralai/Mistral-7B-v0.1",
      "arch_family": "mistral",
      "param_b": 7.2,
      "expected_hidden_dim": 4096,
      "auth_required": false,
      "dtype": "bf16",
      "substitution_note": "Replaces meta-llama/Llama-3.1-8B (gated) — diversity maintained (Mistral vs Qwen3 arch)"
    },
    "p3": {
      "model": "Qwen/Qwen2.5-14B",
      "arch_family": "qwen2_5",
      "param_b": 14.7,
      "expected_hidden_dim": 5120,
      "auth_required": false,
      "dtype": "bf16",
      "substitution_note": "Replaces mistralai/Ministral-3-14B (multimodal config rejected by AutoModelForCausalLM)"
    },
    "p4": {
      "model": "mistralai/Mistral-Nemo-Base-2407",
      "arch_family": "mistral_nemo",
      "param_b": 12.2,
      "expected_hidden_dim": 5120,
      "auth_required": false,
      "dtype": "bf16",
      "substitution_note": "Replaces google/gemma-4-31B (multimodal + bnb 4bit incompat) — keeps 5120 dim class"
    }
  },

  "metric": {
    "primary": "gram_spectrum",
    "secondary": "participation_ratio",
    "gram_spectrum": {
      "eigenvalue_sort": "descending",
      "normalize": "sum_to_one",
      "pair_distance_L2": "euclidean",
      "pair_distance_KL": "forward_kl_with_eps",
      "eps": 1.0e-9
    },
    "participation_ratio": {
      "formula": "(sum(lambda))^2 / sum(lambda^2)",
      "sanity_max_ratio": 2.0
    }
  },

  "threshold": {
    "method": "null_bootstrap",
    "n_bootstrap": 100,
    "shuffle_within_path": true,
    "percentile": 95,
    "fallback_fixed_threshold": 0.05,
    "fallback_reason": "Used only if bootstrap fails (e.g., < 10 successful samples). Real gate is null-distribution p95."
  },

  "output": {
    "pod_raw_capture": "state/h_last_raw_p{path_id}.json",
    "operator_cross_result": "state/phi_4path_cross_result.json",
    "operator_cross_schema": "anima/phi_4path_cross_result/2"
  },

  "exit_criteria": {
    "substrate_indep_conditions": [
      "all 6 L2 pair distances < null p95 L2 threshold",
      "all 6 KL pair distances < null p95 KL threshold",
      "PR_max / PR_min < 2.0 sanity ratio"
    ],
    "fail_followup_trigger": "roadmap #90 option B (CCA) or D (task-trained LoRA)"
  }
}
