{
  "version": 1,
  "description": "airgenome keyword auto-dispatch mapping — cluster #34 improvement 22. Substring match on event payload fields (type+message+tags concatenated). Dry-run default; --apply to execute. Tool paths are placeholders — dispatcher emits plan only, caller/executor resolves. Mirrored also at shared/config/ (local-only namespace).",
  "dry_run_default": true,
  "rules": [
    {
      "keyword": "h100_idle",
      "tool": "tool/h100_auto_kill.hexa",
      "args": ["--reason", "airgenome:h100_idle"],
      "note": "GPU idle >threshold → auto-kill stale job"
    },
    {
      "keyword": "cert_updated",
      "tool": "tool/meta2_hashchain_verify.hexa",
      "args": ["--verify"],
      "note": "cert chain change → meta² hashchain re-verify"
    },
    {
      "keyword": "corpus_added",
      "tool": "tool/alm_corpus_4gate.hexa",
      "args": ["--gate", "all"],
      "note": "new corpus → 4-gate validation"
    }
  ]
}
