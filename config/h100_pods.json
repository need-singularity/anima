{
  "_comment": "H100 pod registry for auto-kill guard. Add one entry per rented pod. last_activity_source ∈ {who, tmux, nvidia_smi}. ssh_host empty string disables remote probe (local self-test / stub mode).",
  "pods": [
    {
      "pod_id": "stub-pod-001",
      "provider": "runpod",
      "ssh_host": "",
      "ssh_port": 22,
      "ssh_user": "root",
      "last_activity_source": "nvidia_smi",
      "notes": "Example stub — replace ssh_host with real pod address before --apply."
    }
  ]
}
