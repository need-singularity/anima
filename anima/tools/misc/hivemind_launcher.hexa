// hivemind_launcher.hexa — Start N Anima nodes + Gateway as Hivemind
// Original: hivemind_launcher.py

fn compute_auto_nodes(available_mb: int) -> int {
    // TODO[pytorch]: max(1, available_mb / 600)
    1
}

fn launch_nodes(n: int, mode: string) -> opaque {
    // TODO[pytorch]: spawn N anima processes (subprocess or docker)
    0
}

fn launch_gateway(node_ports: opaque) {
    // TODO[pytorch]: start HivemindGateway with node ports
}

fn write_discovery(nodes: opaque, path: string) {
    // TODO[pytorch]: write node info to /tmp/anima_hivemind/nodes.json
}

fn shutdown_all(pids: opaque) {
    // TODO[pytorch]: graceful SIGTERM to all nodes + gateway
}

fn main() {
    // TODO[pytorch]: argparse for nodes/auto/mode(docker), launch + gateway
    print("hivemind_launcher stub")
}
