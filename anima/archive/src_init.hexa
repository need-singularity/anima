// archive/src_init.hexa — Legacy/deprecated modules moved from active codebase.
// Import via backward-compatible re-exports in their original modules.

// Module declaration: archive
// This module is frozen — no imports, no modifications.
// Resurrection requires copy + modernization.
