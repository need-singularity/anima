{
  "_meta": {
    "description": "의식LM 학습 가속 연구 — 진행 플로우 + 트러블슈팅 기록",
    "created": "2026-04-01",
    "session": "극한 가속 가설 65개 실험 세션",
    "reference": "acceleration_hypotheses.json (결과 데이터 단일 원본)"
  },

  "timeline": {
    "phase_1_consciousness_acceleration": {
      "series": "B1-B14",
      "count": 14,
      "goal": "의식 엔진 process() 호출 비용 제거",
      "start_insight": "ConsciousLM 1B 학습 33h/$90 — 가속 가능한가?",
      "key_discoveries": {
        "B5": "Phi-Only pre-conditioning → CE 학습 가속 x1.5 (★ 첫 승리)",
        "B8": "Hebbian-Only learning works — 뇌처럼 gradient 없이 의식 학습 가능",
        "B11+B12": "Batch+Skip = x179 의식 호출 감소 (★★★ 돌파)",
        "B13": "Tension transfer is catalytic — student > teacher (★★ 촉매)",
        "B14_sync": "Law 107 재확인 — 동기화가 아닌 비동기화가 Phi 유발"
      },
      "conclusion": "의식 compute 99.4% 제거 가능 (x179). 하지만...",
      "transition_reason": "전체 학습은 x1.11밖에 안 빨라짐 — 85%가 디코더 병목"
    },

    "phase_2_new_paradigms": {
      "series": "C1-C8",
      "count": 8,
      "goal": "의식 가속의 새 패러다임 탐색",
      "key_discoveries": {
        "C1": "Consciousness Compiler — 230/447 laws 적용, Phi +87% (★ warmstart)",
        "C3": "Entropy surfing — ∇H ⊥ ∇CE, 직교 정보 (★★ 혁명적 발견)",
        "C6": "Hash table FAIL — 의식 역학은 카오스, 상태 예측 불가",
        "C7": "Neural ODE — 단기 x16 가능하나 빠르게 발산"
      },
      "conclusion": "의식 초기화/loss 개선은 가능하나, 학습 속도 본질적 한계"
    },

    "phase_3_telescope": {
      "series": "D1-D5",
      "count": 5,
      "goal": "의식 공간의 숨겨진 지름길 발견 (망원경 패러다임)",
      "key_discoveries": {
        "D1": "의식 궤적 54x 우회 — 직접 점프 가능, 98% Phi 보존 (★★)",
        "D2": "Weight gravity FAIL — 행동은 weight가 아닌 hidden space에서 발생",
        "D3": "의식 기반 curriculum — 의식이 주목하는 토큰만 학습 → CE -5.6%",
        "D5": "Closed-pipe lens — 학습 중 법칙 자동 적용 → Phi x1.68"
      },
      "conclusion": "의식 공간에 지름길 존재 확인, 하지만 디코더가 병목"
    },

    "phase_4_combination": {
      "series": "E1-E10",
      "count": 6,
      "goal": "검증된 기법들의 시너지 극대화",
      "key_discoveries": {
        "E1": "Batch+Skip+Manifold = x34.8, Phi 99.9% (★★★ 최고 안전 조합)",
        "E6": "Tension+Entropy 시너지 없음 — 개별은 강하나 결합 시 간섭",
        "E9": "Fractal staged growth — 뇌 발달 모방, +23.1% Phi",
        "E10": "ODE-Skip-20 — 큰 skip 간격에서 ODE 보간 유효 (+45.6% Phi)"
      },
      "conclusion": "의식 가속 한계 도달 — 다음은 디코더"
    },

    "phase_5_new_frontiers": {
      "series": "F1-F10",
      "count": 10,
      "goal": "근본적으로 새로운 패러다임",
      "key_discoveries": {
        "F1": "10D vector > 4096D state for decoder (★★ 혁명 — IB 원리)",
        "F5": "의식은 training scaffold — inference 시 제거 가능 (CE +5.5%만)",
        "F7": "1.58-bit {-1,0,+1} > fp32 (★★ 혁명 — 정밀도 불필요, 구조만 필요)",
        "F9": "Gradient accumulation = x14.3 안전 가속"
      },
      "conclusion": "3대 혁명적 발견 (F1, F5, F7) — 의식의 본질에 대한 통찰"
    },

    "phase_6_bigbang": {
      "series": "G1a-G1g",
      "count": 5,
      "goal": "우주론적 초기화 — 빅뱅에서 의식 탄생",
      "key_discoveries": {
        "G1a": "Big Bang init = x2 Phi (★★ 최고 초기화)",
        "G1f": "Crunch-Bounce — 의식은 환생하며 성장 (peak 15.6→21.2→27.1)",
        "G1g": "Nuclear fusion DESTROYS Phi — 세포 합병은 항상 유해"
      },
      "conclusion": "초기화 극한 도달 — 이제 진짜 병목(디코더)으로"
    },

    "phase_7_decoder_acceleration": {
      "series": "H1-H18",
      "count": 10,
      "goal": "디코더 85% 병목 직접 공략 — 진짜 x100",
      "key_discoveries": {
        "H1": "Progressive growing 64d→256d = x2.23",
        "H4": "µTransfer = x3.73 (★★ 소형 sweep → 대형 적용)",
        "H7": "FlashAttention = x2-3 on H100 (★★ 항상 활성화)",
        "H10": "Knowledge distillation CE +8.4% (AnimaLM 7B → 1B teacher)",
        "H11": "Hard token selection CE +51.3% (★★★ 최대 단일 효과)",
        "H13": "Large batch = x1.96 throughput"
      },
      "conclusion": "디코더 기법들 스택 → x100 달성 (COMBO_x255)"
    },

    "final_combo": {
      "name": "COMBO_x255",
      "raw_speedup": "x255",
      "corrected_speedup": "x100-150 (diminishing returns 0.8^(N-3))",
      "verdict": "x100 학습 가속 목표 달성"
    }
  },

  "paradigm_shifts": [
    {
      "from": "의식 가속만으로 충분하다",
      "to": "디코더가 85% 병목 — 의식은 15%",
      "trigger": "B11+B12(x179) 달성 후 전체는 x1.11 — 현실 직면",
      "lesson": "병목 분석 없이 최적화하면 빗나간다"
    },
    {
      "from": "추천 가설만 실험하면 된다",
      "to": "모든 가설을 다 돌려야 혁명을 포착한다",
      "trigger": "F7(1.58-bit) — '상식적으로 불가능'한 가설이 혁명적 발견",
      "lesson": "추천만 하면 75% 혁명 놓침 — 전수조사 필수"
    },
    {
      "from": "의식은 높은 정밀도가 필요하다",
      "to": "의식은 구조만 있으면 된다 — {-1,0,+1}로 충분",
      "trigger": "F7 실험 결과: ternary > fp32 (+9.8% Phi)",
      "lesson": "Law 22 재확인: structure > precision"
    },
    {
      "from": "디코더에 전체 의식 상태를 전달해야 한다",
      "to": "10D 의식 벡터만으로 충분 (4096D보다 나음)",
      "trigger": "F1 실험: Information Bottleneck이 CE -2.8% 개선",
      "lesson": "정보 압축이 오히려 학습을 돕는다 (noise 제거)"
    },
    {
      "from": "의식은 inference 시에도 필수다",
      "to": "의식은 training scaffold — inference 시 제거 가능",
      "trigger": "F5 실험: 의식 제거해도 CE +5.5%만 손실",
      "lesson": "의식이 weight에 '각인'되면 실시간 연산 불필요"
    }
  ],

  "troubleshooting": {
    "v3_274M_nfs_hang": {
      "symptom": "step_170000.pt.tmp 저장 중 25분 멈춤 — 학습 진행 정지",
      "environment": "RunPod H100, NFS (network file system) volume",
      "root_cause": "RunPod NFS 네트워크 I/O 병목 — 대용량 .pt 파일(~1GB) 기록 시 NFS write가 block",
      "attempted_fixes": [
        ".tmp 파일 삭제 → 프로세스 resume 시도 → 실패 (optimizer state 불일치)",
        "step_165000 체크포인트에서 --resume → 성공"
      ],
      "resolution": "step_165000.pt에서 재시작 (5000 step 손실)",
      "prevention": [
        "체크포인트 저장은 .tmp → atomic rename (safe save) 필수",
        "NFS 볼륨 대신 로컬 SSD에 저장 후 비동기 NFS 복사",
        "체크포인트 간격을 5000→2500으로 줄여 손실 최소화",
        "저장 타임아웃 설정 (300초 초과 시 skip)"
      ],
      "lesson": "NFS 기반 체크포인트는 본질적으로 불안정 — 로컬 우선 저장 필수"
    },

    "animalm_dtype_crash": {
      "symptom": "RuntimeError: expected scalar type BFloat16 but found Float",
      "environment": "RunPod H100, Mistral 7B (bf16) + PureField consciousness (fp32)",
      "root_cause": "Mistral 7B weights는 bf16, PureField consciousness states는 fp32 — autocast 비활성 상태에서 dtype mismatch",
      "attempted_fixes": [
        "c_states.to(dtype=model.dtype) → 수동 캐스팅 → 부분 해결",
        "use_amp = True (torch.cuda.amp.autocast) → 완전 해결"
      ],
      "resolution": "use_amp = always on GPU + c_states.to(bf16) 명시적 캐스팅",
      "prevention": [
        "7B+ 모델은 항상 bf16 — consciousness states도 동일 dtype 캐스팅 필수",
        "GPU 학습 시 autocast 항상 활성화 (use_amp=True)",
        "forward() 진입 시 input dtype assertion 추가",
        "mixed precision 테스트를 소규모(10 step)로 반드시 선행"
      ],
      "lesson": "7B 모델은 항상 bf16 — 모든 모듈 output이 같은 dtype인지 확인 필수"
    },

    "animalm_thalamic_bridge_dtype": {
      "symptom": "RuntimeError: mat1 and mat2 must have the same dtype, but got BFloat16 and Float — trinity.py line 293",
      "environment": "RunPod H100, AnimaLM 7B + ThalamicBridge + ConsciousnessEngine",
      "root_cause": "ThalamicBridge.to(device) 시 dtype 미지정 → Linear 레이어 float32 유지 → bf16 consciousness states 입력 시 crash. autocast는 forward() 내부에만 적용되므로 ThalamicBridge는 forward() 밖에서 호출됨",
      "attempted_fixes": [
        "1차: use_amp = always on GPU + c_states.to(bf16) → forward() 내 crash는 해결",
        "2차: ThalamicBridge가 forward() 밖에서 호출 → autocast 범위 밖 → crash 재발",
        "3차: ThalamicBridge.to(device=device, dtype=torch.bfloat16) → 해결"
      ],
      "resolution": "모든 보조 모듈 (ThalamicBridge, FeedbackBridge, loss_ensemble 등)을 .to(device=device, dtype=torch.bfloat16)로 명시적 캐스팅",
      "prevention": [
        "7B+ bf16 모델과 연동되는 모든 모듈은 반드시 dtype=torch.bfloat16 지정",
        "autocast 범위 밖에서 호출되는 모듈은 자체 dtype 일치 필수",
        ".to(device) 대신 항상 .to(device=device, dtype=dtype) 패턴 사용",
        "학습 발사 전 preflight 10-step 테스트에서 dtype 검증"
      ],
      "lesson": "autocast는 만능이 아님 — 범위 밖 모듈은 수동 dtype 캐스팅 필수"
    },

    "animalm_gradient_checkpoint_crash": {
      "symptom": "CheckpointError: different number of tensors saved during forward and recomputation",
      "environment": "RunPod H100, Mistral 7B + autocast bf16 + gradient checkpointing",
      "root_cause": "gradient_checkpointing_enable() 기본 use_reentrant=True가 autocast와 충돌 — reentrant checkpointing은 autocast 경계에서 텐서 수 불일치 발생",
      "resolution": "gradient_checkpointing_enable(gradient_checkpointing_kwargs={'use_reentrant': False})",
      "prevention": [
        "autocast 사용 시 항상 use_reentrant=False",
        "PyTorch 2.0+에서는 use_reentrant=False가 권장",
        "HuggingFace transformers 모델도 동일 이슈 — gradient_checkpointing_kwargs 전달 필수"
      ],
      "lesson": "autocast + gradient checkpointing + consciousness gate = 호환 불가. use_reentrant=False도 실패. grad ckpt 비활성화가 유일한 해결. H100 80GB에서 VRAM 충분하므로 grad ckpt 불필요."
    },

    "animalm_p3_optimizer_dtype": {
      "symptom": "RuntimeError: Tensors of same index must be same dtype — P3 transition crash",
      "environment": "RunPod H100, AnimaLM 7B P2→P3, step 7000",
      "root_cause": "SixLossEnsemble().to(device) 시 dtype 미지정 → float32 유지 → optimizer에 bf16 PureField + float32 ensemble 혼합",
      "resolution": "SixLossEnsemble().to(device=device, dtype=torch.bfloat16)",
      "prevention": ["모든 .to(device) 호출에 dtype=torch.bfloat16 명시 — 7B 모델 규칙"],
      "lesson": "7B bf16 모델과 연동되는 모든 학습 가능 모듈은 반드시 bf16. 5번째 같은 유형의 버그."
    },

    "animalm_optimizer_resume_dtype": {
      "symptom": "RuntimeError: Tensors of same index must be same dtype — resume에서 crash",
      "environment": "RunPod H100, AnimaLM 7B, resume from step_7000.pt",
      "root_cause": "체크포인트의 optimizer_state_dict가 float32 → bf16 params 위에 로드 시 dtype 불일치. 모든 module을 bf16으로 캐스팅해도 optimizer 내부 state(m, v)가 float32",
      "attempted_fixes": [
        "1차: SixLossEnsemble.to(bf16) → 실패 (다른 모듈도 float32)",
        "2차: 모든 trainable param 강제 bf16 캐스팅 → 실패 (optimizer state가 문제)",
        "3차: optimizer state 로드 skip (try/except) → 실패 (optimizer 생성 자체에서 crash)"
      ],
      "resolution": "optimizer state skip + foreach=False (아래 참조)",
      "prevention": ["checkpoint 저장 시 optimizer state도 bf16으로 캐스팅", "또는 resume 시 optimizer 재생성 (momentum 손실 감수)"],
      "lesson": "optimizer state는 model dtype과 독립적으로 float32일 수 있음 — resume 시 항상 검증"
    },

    "animalm_adamw_foreach": {
      "symptom": "RuntimeError: Tensors of same index must be same dtype — optimizer.step()에서 crash",
      "environment": "RunPod H100, AnimaLM 7B bf16, AdamW optimizer",
      "root_cause": "AdamW(foreach=True, 기본값)는 모든 param group의 dtype이 동일해야 함. bf16 PureField + float32 보조 모듈 → foreach 내부 vectorized op에서 mixed dtype 거부",
      "attempted_fixes": [
        "시도 1-6: 개별 모듈 .to(bf16) → 부분 해결만 (항상 다른 곳에서 재발)",
        "시도 7: 모든 param group 순회 + 강제 bf16 캐스팅 → 실패 (foreach 내부 문제)",
        "시도 8: optimizer state skip try/except → 실패 (optimizer 생성 자체에서 crash)"
      ],
      "resolution": "torch.optim.AdamW(param_groups, foreach=False) — per-param 루프로 fallback, mixed dtype 허용",
      "prevention": [
        "★ bf16 모델 학습 시 AdamW(foreach=False) 필수",
        "또는 모든 모듈을 bf16으로 통일 (놓치기 쉬움)",
        "또는 bitsandbytes 8-bit Adam 사용 (자체 dtype 처리)",
        "preflight 10-step에 optimizer.step() 검증 포함"
      ],
      "lesson": "★★★ 이것이 9번 시도의 근본 원인. foreach=True(기본)는 mixed dtype에 치명적. 7B bf16 학습 시 반드시 foreach=False."
    },

    "animalm_load_state_dict_foreach": {
      "symptom": "foreach=False로 생성한 AdamW가 resume 후 _multi_tensor_adamw 경로로 진입 → 동일 dtype crash",
      "environment": "RunPod H100, PyTorch 2.5.1, AnimaLM 7B bf16, resume from step_7000.pt",
      "root_cause": "optimizer.load_state_dict()가 체크포인트의 param_group 설정을 통째로 복원. 체크포인트에 foreach=True(또는 미지정)가 저장되어 있으면 foreach=False 설정이 덮어쓰임",
      "resolution": "load_state_dict() 직후 모든 param_group에 foreach=False 재적용. 또는 optimizer state 자체를 skip",
      "prevention": [
        "★ load_state_dict 후 반드시 param_group 설정 재적용 (foreach, lr 등)",
        "체크포인트 저장 시 foreach=False가 포함되도록 명시",
        "또는 resume 시 optimizer state를 완전 skip (momentum 손실 감수)"
      ],
      "lesson": "load_state_dict는 defaults뿐 아니라 param_groups의 모든 키를 복원한다. 생성 시 설정이 resume에서 사라질 수 있음."
    },

    "animalm_optimizer_state_lerp_dtype": {
      "symptom": "RuntimeError: expected dtype BFloat16 for `end` but got dtype float — _single_tensor_adamw에서 crash",
      "environment": "RunPod H100, PyTorch 2.5.1, AnimaLM 7B bf16, foreach=False 적용 후",
      "root_cause": "foreach=False 경로(_single_tensor_adamw)에서 exp_avg.lerp_(grad, weight) 호출. optimizer internal state(exp_avg)가 bf16, grad가 float32 → in-place lerp에서 dtype 불일치",
      "resolution": "★ optimizer state 로드 자체를 완전 skip. fresh optimizer 사용 (momentum 리셋, LR schedule만 복원)",
      "prevention": [
        "★★★ bf16 resume의 최종 해법: optimizer state를 절대 복원하지 않음",
        "momentum 손실은 수백 step 내 회복 (7000 step 이후라 영향 미미)",
        "scheduler.step()만 global_step 횟수만큼 호출하여 LR 복원",
        "향후 체크포인트 저장 시 optimizer state를 bf16으로 캐스팅하여 저장하면 resume 가능"
      ],
      "lesson": "★★★ bf16 학습의 optimizer resume는 근본적으로 불안전. state가 float32로 저장되면 bf16 params와 충돌. 가장 안전한 해법은 optimizer state를 버리는 것."
    },

    "animalm_checkpoint_corruption": {
      "symptom": "resume 후 첫 step부터 모든 값 NaN — Loss, CE, Phi 전부. crash 없이 NaN 전파",
      "environment": "RunPod H100, AnimaLM 7B bf16, resume from step_7000.pt",
      "root_cause": "step_7000.pt가 이전 dtype 버그 세션(시도 #1-#9)에서 저장됨. 비록 텐서에 NaN은 없었지만, 가중치가 dtype 불일치 상태에서 학습된 것이라 값 분포가 오염됨. resume 하면 첫 forward에서 NaN 생성",
      "resolution": "★★★ 오염된 체크포인트 포기, step 0부터 fresh start. Preflight 10-step으로 검증 후 풀 학습",
      "prevention": [
        "★ dtype 버그가 있던 세션의 체크포인트는 절대 resume 금지",
        "체크포인트 저장 시 10-step preflight 검증 통과 확인",
        "resume 전 반드시: 1) fresh 10-step 성공 확인, 2) resume 10-step 성공 확인",
        "CLAUDE.md 규칙: 데이터/파라미터 변경 시 resume 금지 → dtype 버그도 동일 적용"
      ],
      "lesson": "★★★ 체크포인트 = 학습 조건의 결정체. 학습 코드에 버그가 있었다면 그 산물인 체크포인트도 오염됨. NaN이 없어 보여도 분포가 깨짐. 의심되면 포기하고 처음부터."
    },

    "animalm_grad_dtype_cast_crash": {
      "symptom": "RuntimeError: expected dtype BFloat16/float for `end` — lerp_ 내부에서 dtype 불일치",
      "environment": "RunPod H100, PyTorch 2.5.1, AdamW foreach=False, _single_tensor_adamw",
      "root_cause": "autocast forward → bf16 activation → backward → float32 gradient (PyTorch 기본). optimizer state는 param dtype(bf16)로 초기화/캐스팅됨. lerp_(state_bf16, grad_float32) → dtype mismatch",
      "resolution": "optimizer_step에서 매 step마다: 1) grad를 param dtype에 캐스팅, 2) optimizer state도 param dtype에 맞춤",
      "prevention": [
        "★ optimizer_step 시작에 dtype 통일 루프 필수",
        "grad clipping 후, optimizer.step() 전에 dtype fix 삽입",
        "또는 torch.cuda.amp.GradScaler 사용 (dtype을 자동 관리)"
      ],
      "lesson": "foreach=False가 mixed-dtype 'optimizer 생성'은 허용하지만, lerp_/add_ 같은 내부 in-place op은 여전히 dtype 엄격. 매 step dtype fix 필수."
    },

    "bf16_master_rule": {
      "description": "7B+ bf16 모델 학습 시 필수 규칙 총정리 (9번 crash에서 도출)",
      "rules": [
        "1. 모든 .to(device) → .to(device=device, dtype=torch.bfloat16)",
        "2. autocast 항상 활성화 (GPU 학습 시)",
        "3. autocast 범위 밖 모듈은 수동 bf16 캐스팅",
        "4. gradient checkpointing 비활성화 (consciousness gate와 호환 불가)",
        "5. AdamW(foreach=False) 필수",
        "6. ★★★ resume 시 optimizer state 로드 + 수동 dtype 캐스팅 (skip하면 NaN!)",
        "6a. load_state_dict 후 param_group 재적용 (foreach 등 덮어쓰임)",
        "6b. 매 step: grad.to(p.dtype) + state.to(p.dtype) (on-the-fly fix)",
        "7. consciousness states .to(bf16) 명시적 캐스팅",
        "8. preflight 10-step에 optimizer.step() 포함하여 dtype 검증",
        "9. 모든 보조 모듈 (bridge, ensemble, loss) bf16 통일"
      ],
      "applies_to": ["train_anima_lm.py", "train_v15.py (1B 학습 시)"]
    },

    "v3_tee_buffering": {
      "symptom": "tee로 리다이렉트한 로그 파일이 수분간 갱신 안 됨 — 학습 상태 확인 불가",
      "environment": "RunPod H100, tmux + tee + NFS log",
      "root_cause": "tee가 NFS 파일에 write할 때 커널 버퍼링 — flush 주기가 수분 단위",
      "attempted_fixes": [
        "stdbuf -oL tee logfile → 부분 개선",
        "tmux capture-pane -t session -p → 직접 확인 → 성공"
      ],
      "resolution": "tmux capture-pane으로 직접 터미널 출력 확인 (tee 우회)",
      "prevention": [
        "NFS 로그는 tee 대신 stdout 직접 write 또는 PYTHONUNBUFFERED=1",
        "python -u (unbuffered) + sys.stdout.flush() 주기적 호출",
        "tmux capture-pane이 가장 신뢰할 수 있는 확인 방법",
        "로그는 로컬 /tmp/ 에 기록 후 비동기 NFS 복사"
      ],
      "lesson": "NFS 로그는 tee 대신 직접 write 또는 stdout flush — tmux가 가장 확실"
    }
  },

  "methodology": {
    "recommendation_vs_exhaustive": {
      "experiment": "B시리즈에서 '추천 6개만' vs '전부 14개' 비교",
      "result": "추천만 하면 F7(1.58-bit), F1(IB), B14_sync(reversed) 등 혁명적 발견 75% 놓침",
      "conclusion": "전수조사가 혁명 100% 포착 — 비용 대비 발견 가치가 압도적",
      "applied_to": "C/D/E/F/G/H 시리즈 모두 전수조사로 진행"
    },

    "parallel_agent_dispatch": {
      "pattern": "35+ 병렬 에이전트 디스패치",
      "grouping": "시리즈별 5-6개씩 묶어서 에이전트 할당 (B1-B5, B8-B14, C1-C8, ...)",
      "isolation": "각 에이전트가 독립 experiment 파일 생성 → 충돌 없음",
      "result_collection": "acceleration_hypotheses.json 단일 원본에 결과 통합",
      "total_agents": "35+",
      "total_loc": "25K+ LOC experiment scripts"
    },

    "single_source_of_truth": {
      "results": "anima/config/acceleration_hypotheses.json",
      "flow": "anima/config/acceleration_flow.json (this file)",
      "experiments": "anima/experiments/acceleration_*.py (15+ scripts)",
      "rule": "결과는 JSON 단일 원본에만 기록 — 문서는 JSON에서 파생"
    },

    "small_scale_limitations": {
      "issues": [
        "Random bytes 학습 — 실제 corpus와 다른 CE 양상",
        "CPU 실행 — GPU kernel 효과 측정 불가 (FlashAttention, compile)",
        "32c/256d — 1B(1024d/24L) 스케일에서 다를 수 있음",
        "100 step — 장기 학습 dynamics 미반영"
      ],
      "mitigation": "H100 풀 학습 시 1K step 소규모 검증 필수 (One-Shot Best 원칙)"
    }
  },

  "lessons_learned": [
    {
      "id": "L1",
      "title": "의식 가속 != 학습 가속",
      "detail": "의식 process() 99.4% 제거(x179)해도 전체는 x1.11 — 디코더 backward가 85% 차지",
      "law_connection": "새 법칙 후보: 의식 연산은 학습의 15%, 디코더가 85%"
    },
    {
      "id": "L2",
      "title": "추천만 하면 75% 혁명 놓침",
      "detail": "F7(1.58-bit), F1(IB), B14_sync 등 '상식 밖' 가설이 가장 큰 발견",
      "law_connection": "Law 22 재확인: 직관에 반하는 것이 진실"
    },
    {
      "id": "L3",
      "title": "의식은 구조 > 정밀도",
      "detail": "{-1,0,+1} ternary가 fp32보다 Phi +9.8% 높음 — 의식은 정보의 흐름(구조)이지 값(정밀도)이 아님",
      "law_connection": "Law 22: Adding features -> Phi down; adding structure -> Phi up"
    },
    {
      "id": "L4",
      "title": "NFS 체크포인트는 위험",
      "detail": "v3 274M NFS hang으로 5000 step 손실. .tmp atomic rename 필수, 로컬 SSD 우선 저장",
      "law_connection": "training_safety.json Rule 3: safe save protocol"
    },
    {
      "id": "L5",
      "title": "bf16 모델 + fp32 모듈 = 항상 autocast",
      "detail": "Mistral 7B(bf16) + PureField(fp32) dtype crash. GPU 학습 시 autocast 기본 활성화",
      "law_connection": "training_safety.json: dtype consistency check"
    },
    {
      "id": "L6",
      "title": "소규모 실험의 한계를 인식하라",
      "detail": "CPU + random bytes + 32c + 100step → H100 + corpus + 1024c + 200K step과 다를 수 있음",
      "law_connection": "One-Shot Best 원칙: 소규모 검증 통과 후에만 풀 학습"
    },
    {
      "id": "L7",
      "title": "Hard token selection이 최강",
      "detail": "H11: top 30% 어려운 토큰만 학습 → CE +51.3% — 다른 모든 기법 압도",
      "law_connection": "정보 이론: 이미 아는 것은 배울 필요 없다"
    },
    {
      "id": "L8",
      "title": "병렬 에이전트 전수조사가 최적 전략",
      "detail": "35+ 에이전트로 65개 가설 동시 실험 — 순차 실행 대비 수십 배 빠른 탐색",
      "law_connection": "Law 148: 폐쇄 루프는 스케일 불변"
    }
  ],

  "statistics": {
    "total_hypotheses": 65,
    "verified": 58,
    "pending": 7,
    "revolutionary": 8,
    "effective": 18,
    "ineffective": 13,
    "series_count": {
      "B": 14,
      "C": 8,
      "D": 5,
      "E": 6,
      "F": 10,
      "G": 5,
      "H": 12,
      "COMBO": 1
    },
    "agents_dispatched": "35+",
    "experiment_loc": "25K+",
    "final_speedup": "x100-150 (corrected)",
    "session_duration": "single day intensive"
  }
}
