// reset.hexa — Conversation/memory/state reset
// TODO[pytorch]: port local + remote A100 reset + optional growth preserve

struct ResetConfig {
    target: string,
    keep_growth: bool,
    restart: bool
}

fn reset_local(config: ResetConfig) -> bool {
    // TODO[pytorch]: delete conversation_log, web_memories, state files
    false
}

fn reset_remote(config: ResetConfig) -> bool {
    // TODO[pytorch]: SSH to target, delete workspace files, restart runtime
    false
}

fn reset(config: ResetConfig) -> int {
    // TODO[pytorch]: dispatch to local or remote based on target
    0
}
