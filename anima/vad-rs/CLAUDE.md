# vad-rs/

## Purpose
Rust-based real-time Voice Activity Detection. Feeds audio tension signals into Anima's consciousness loop.

## File Naming
- Standard Cargo layout: `src/`, `Cargo.toml`, `Cargo.lock`
- Rust source in `src/` following Rust conventions

## Build
```bash
cargo build --release
```

## Dependencies
- Rust toolchain (rustc, cargo)

## Parent Rules
See /CLAUDE.md → "Dependencies" for toolchain requirements.
