// _split_target.hexa — sync/ split target marker
// Received: anima/core/runtime/cloud_sync.py (1,005 lines — largest split)
// Classification: L1 (protected) — R2 checkpoint sync, IO-heavy
// Interface:
//   - CloudSync(bucket, creds) — R2 upload/download orchestrator
//   - push_checkpoint(path), pull_latest(), verify_integrity()
//   - bg_sync_daemon() — background sync loop
// Dependencies: boto3, secret_vault (L0, core/runtime/), runpod_manager (modules/cloud/)
// Consumers: deploy.py, training entry points, R2 backup policy
// Note: IO-heavy, slow tests — isolating here lets core/runtime/ stay fast to import.

fn _marker() -> int { 0 }
