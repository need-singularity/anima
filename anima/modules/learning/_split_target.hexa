// _split_target.hexa — learning/ split target marker
// Receives: anima/core/runtime/autonomous_loop.py (785 lines)
// Classification: L1 (protected) — long-horizon autonomous learning loop
// Interface:
//   - AutonomousLoop(hub, memory) — topic selection + web search + learn
//   - tick() — one autonomous cycle
//   - select_topic(), search_and_learn() — sub-phases
// Dependencies: consciousness_hub (core, L0), duckduckgo_search, memory store
// Consumers: babysitter (education/), top-level daemons
// Note: core/runtime/autonomous_loop.hexa stub already exists — Python impl moves here.

fn _marker() -> int { 0 }
