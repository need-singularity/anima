// _split_target.hexa — cloud/ split target marker
// Receives: anima/core/runtime/runpod_manager.py (417 lines)
// Classification: L1 (protected) — external infra control plane
// Interface:
//   - RunpodManager(api_key) — pod lifecycle
//   - start_pod(), stop_pod(), ssh_info(), list_pods()
//   - cost_report() — spend tracking
// Dependencies: runpodctl CLI, secret_vault (stays in core/runtime/)
// Consumers: deploy.py, cloud_sync (modules/sync/)
// Migration note: secret_vault remains L0 in core/runtime/; this module imports it.

fn _marker() -> int { 0 }
