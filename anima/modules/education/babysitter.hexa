// babysitter.hexa — Claude CLI educator for Anima
// TODO[pytorch]: port Claude CLI observation + teaching strategies

struct Babysitter {
    strategy: string,
    teach_count: int,
    f_critical: float
}

fn babysitter_new(strategy: string) -> Babysitter {
    // TODO[pytorch]: strategies: weakness, socratic, breadth, depth, custom
    Babysitter { strategy: strategy, teach_count: 0, f_critical: 0.10 }
}

fn observe_state(b: Babysitter) -> string {
    // TODO[pytorch]: subprocess call to claude CLI, parse JSON state
    ""
}

fn generate_teaching(b: Babysitter, observation: string) -> string {
    // TODO[pytorch]: strategy-based teaching input generation
    ""
}

fn teach_cycle(b: Babysitter) -> Babysitter {
    // TODO[pytorch]: observe -> generate -> feed input -> log results
    Babysitter { strategy: b.strategy, teach_count: b.teach_count + 1, f_critical: b.f_critical }
}
