// _split_target.hexa — education/ split target marker
// Receives: anima/core/runtime/babysitter.py (168 lines)
// Classification: L1 (protected) — curriculum auto-tuner, training-critical
// Interface:
//   - Babysitter(model, curriculum) — auto curriculum stepper
//   - tick() — per-step intervention
//   - on_loss_stall(), on_phi_drop() — reactive hooks
// Dependencies: live_tuner (modules/training/), autonomous_loop (modules/learning/)
// Note: cross-module deps both live outside core/runtime/ after split — no cycle.
// Migration note: import path changes from
//   anima.core.runtime.babysitter -> anima.modules.education.babysitter

fn _marker() -> int { 0 }
