// _split_target.hexa — monitor/ split target marker
// Receives: anima/core/runtime/training_dashboard.py (155 lines)
// Classification: L2 (free) — read-only TUI
// Interface:
//   - TrainingDashboard(ckpt_dir) — live checkpoint viewer
//   - render() — rich/textual rendering loop
//   - main: python -m anima.modules.monitor.training_dashboard
// Dependencies: rich or textual, checkpoint dir (read-only)
// Side effects: none (read-only filesystem scans)
// Migration note: no core/runtime/ code imports this; safe to move in isolation.

fn _marker() -> int { 0 }
