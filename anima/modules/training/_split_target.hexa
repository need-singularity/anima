// _split_target.hexa — training/ split target marker
// Received: anima/core/runtime/live_tuner.py (309 lines)
// Classification: L1 (protected) — active training-loop hyperparam tuner
// Interface:
//   - LiveTuner(config) — hot-reload hyperparam controller
//   - apply(step, metrics) — adjust lr/batch/temp mid-training
//   - snapshot() — persist tuner state
// Dependencies: config/training_config.json, checkpoint hooks
// Consumers: babysitter (education/), train scripts
// Note: modules/training/ directory already exists with README.md — co-locate here.

fn _marker() -> int { 0 }
