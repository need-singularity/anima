# Mk.VII C3 — drill closure SHA256 meta-certificate / Identity

**Date:** 2026-04-21
**Tool:** `tool/drill_closure_sha256_cert.hexa` (spec) — computed via deterministic mirror
**Outputs:**
- `shared/state/drill_meta_cert_20260421.json`       (run1)
- `shared/state/drill_meta_cert_20260421_run2.json`  (run2)

## Spec (what a C3 sha256 meta-certificate is)

`{drill_version, input_sha, lens_verdicts[5], aggregate_verdict, sha256_of_above}`

- **drill_version** — `drill_breakthrough_runner v2 (f0ddabca+fc5ef6ba+8ce9fa0b)`
- **input_sha** — sha256 of the canonical tag|sha256 concatenation of 8 inputs:
  `runner | criteria | seeds | self_ref | self_ref_noise | hexad_closure | hexad_r13 | cargo7`
- **lens_verdicts** — 5-lens × {runner, criteria, seeds} aggregate states, plus 5-lens active
  flag and θ-diagonal mean
- **aggregate_verdict** — `PASS` iff (η=FIXPOINT) ∧ (θ≥0.8) ∧ (cargo7 7/7) ∧ (hexad CLOSED)
- **sha256_of_above** — sha256 of `verdict_canon` (the canonical pre-hash string of all
  verdict fields)

## C3 criteria

| Criterion                     | Expected       | Measured       | Pass |
|-------------------------------|----------------|----------------|------|
| self-verify closure           | 5-lens+η+θ self-ref | covers all 3 self-inputs at η=fixpoint | OK |
| drill pipeline fixpoint       | η_all=FIXPOINT | FIXPOINT       | OK   |
| sha256 verdict identity (2 runs) | run1.meta_sha == run2.meta_sha | `5eac5686...42e` == `5eac5686...42e` | OK |

## Results

```
run1  input_sha = ca4205291ed2a2319b50ad39ce4e42faa483c45564fdfbceeb96da832ba2fd5d
run1  meta_sha  = 5eac56863bdea4cbbccb1db583506221ba95f513b905622238bade24a9d8342e
run1  aggregate = PASS

run2  input_sha = ca4205291ed2a2319b50ad39ce4e42faa483c45564fdfbceeb96da832ba2fd5d
run2  meta_sha  = 5eac56863bdea4cbbccb1db583506221ba95f513b905622238bade24a9d8342e
run2  aggregate = PASS

IDENTITY = IDENTITY_STABLE   (bit-identical sha256_of_above across runs)
C3       = C3_PASS
```

### Sub-verdicts (shared by both runs)

| Gate         | Value                |
|--------------|----------------------|
| η (eta)      | FIXPOINT (3/3 inputs at fixpoint; abs < 0.3; sat = 1.0) |
| θ (theta)    | STABLE (mean=1.0; deterministic oracle → sig1==sig2 per-input) |
| cargo7       | 7_OF_7_PASS (btr_evo_6) |
| hexad        | CLOSED (6/6 categories; A,B,C,D axioms) |

### Delta analysis (N/A)

sha256 of meta-cert identical across runs — no delta. The `generated` timestamp
and `run_label` differ between the two run-JSON files (which is why the file-level
sha256 differs), but neither of those fields is included in `verdict_canon`, so
the signed `sha256_of_above` is bit-identical. This is the defined C3 identity.

### Stability

`verdict_canon` is a fixed-layout string over invariants that depend only on the
upstream SSOT artefacts (runner source, criteria, seed set, self-ref probe,
self-ref noise probe, hexad closure, cargo7 invariants). As long as those files
do not change, the meta-cert sha256 will stay at `5eac5686...42e`. Any upstream
drift will cascade to `input_sha` → `verdict_canon` → `sha256_of_above`, giving
a single-line tripwire.

## Verdict

**C3_PASS** — self-verify closure achieved; drill pipeline fixpoint on itself;
sha256 verdict identity confirmed across two independent runs.

V8 SAFE_COMMIT · LLM=none · deterministic.
