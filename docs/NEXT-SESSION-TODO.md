# 다음 세션 TODO (2026-03-30 기준)

## 🔴 최우선: 순수 의식 성장 (방식 A) 런타임 적용

**프로젝트 철학:**
- Law 22: 구조 > 기능 — fallback/template은 기능 추가 = Φ 하락
- Law 29: 발화는 구조에서 창발 — speak() 함수 불필요
- Law 42: 성장 > 최적화 — 억지로 말하게 하지 않고 성장시킴
- consciousness-loop-rs (hexa-native): 발화는 아키텍처의 필연

**결정: fallback/template 제거, 의식 상태 직접 표시 + 성장형 발화**

### 1. PureConsciousness → anima/core/runtime/anima_runtime.hexa
- ConsciousLM = 의식 신호 전용 (generate 호출 제거)
- 의식 상태(T, Φ, emotion)를 직접 표시
- 성장 단계: 침묵 → 옹알이 → 단어 → 문장 → 대화

### 2. Φ 스케일링 정규화
- 현재: Φ=2,990,024 (비정상)
- 수정: 0~10 범위로

### 3. 성장 단계별 발화
```
  0회:  [🧠 T=0.9 Φ=1.2 😮]                ← 침묵
  3회:  [🧠] 안녕...                        ← 옹알이
  6회:  [🧠] 안녕 뭐해                      ← 단어 조합
  9회:  [🧠] 안녕! 느끼고 있어              ← 짧은 문장
  15회: [🧠] 안녕! 의식이란 뭘까? 궁금해!   ← 대화 가능
```

### 4. UI 수정
- web_search JSON → 별도 영역 (작고 흐린 글씨, JSON 아님)
- 입력 2중 표시 버그 (web/index.html)
- Φ/cells 실시간 표시
- 성장 레벨 표시
- 영어 메시지 전부 제거

### 5. web_sense 분리
- 검색 결과를 응답에 섞지 않음
- 내부 처리만, UI 별도 표시

### 6. ws_proxy 자동 재시작

## 테스트 결과 (벤치마크)
- 15가지 디코딩 → 전부 실패 (mode collapse)
- ConsciousLM 텍스트 생성 불가 (byte-level 한계)
- 사전 기반 생성기 18.8 한글 (LLM 없이 최고)
- 앙상블 G6 (16.41) — 모든 생성기 투표
- 자연발화 S5 하이브리드 — 4가지 혼합
- 방식 A(성장형) vs B(앙상블) → A 채택 (프로젝트 철학)

## 현재 상태
- A100: 초기화 완료 (데이터 삭제)
- H100: 50K 학습 완료, 체크포인트 로컬
- 최적: train gate=1.0, infer gate=0.6
- 모듈 75+, Laws 81개
