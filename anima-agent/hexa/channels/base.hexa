// base.hexa — Channel adapter base, unified interface for all messaging channels
// Port of agent/channels/base.py

// All channel adapters (Telegram, Discord, CLI, Slack, WebSocket) implement this protocol
// Messages normalized to ChannelMessage and routed through AnimaAgent.process_message()

fn ChannelAdapter() {
    // TODO: base protocol for channel adapters
    // channel_name: string identifier
    print("ChannelAdapter stub")
}

fn start(agent) {
    // TODO: start listening for messages
    print("start stub")
}

fn stop() {
    // TODO: cleanup and stop
    print("stop stub")
}

fn send(user_id, text) {
    // TODO: send message to user on this channel
    print("send stub")
}

fn ChannelMessage(channel, user_id, text) {
    // TODO: normalized message structure
    print("ChannelMessage stub")
}

fn main() {
    print("channels/base.hexa — TODO: port from channels/base.py")
}
