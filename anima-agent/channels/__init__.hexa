// Anima Agent Platform -- channel adapters
// Channels route messages between external platforms (Telegram, Discord, CLI, etc.)
// and AnimaAgent.process_message(). See original .py for import wiring.
