{
  "paradigm": "cycle 67 — STUCK self-kill canonical utility landing",
  "cycle": 67,
  "user_request": "STUCK 방지 / 업로드한 로직에서 self-kill / 별도 함수로 재사용",
  "canonical_utility": {
    "path": "tool/anima_stuck_self_kill_lib.hexa",
    "functions": [
      "_stuck_by_runtime_cap(uptime_sec, cap_min) -> bool",
      "_stuck_by_cost_cap_cents(cost_cents, cap_cents) -> bool",
      "_stuck_by_gpu_idle_min(idle_min, threshold_min) -> bool",
      "_stuck_by_heartbeat_age(heartbeat_path, max_age_sec) -> bool",
      "_self_kill_dispatch(pod_id, reason, apply) -> int",
      "_stuck_watchdog_evaluate(any_stuck, reason, pod_id, apply) -> int"
    ],
    "selftest_scenarios": 13,
    "selftest_status": "PASS_13_OF_13",
    "h42_inline_convention": true,
    "side_effects": "dispatch only (subprocess); detect는 모두 read-only",
    "security": "RUNPOD_API_KEY logs에 출력 안 함; pod_id empty시 즉시 1 반환"
  },
  "integration_sites": [
    {
      "site": "tool/h100_post_launch_cascade.hexa",
      "role": "pod-side cascade watchdog",
      "attach_point": "cascade main loop start (background poll loop)",
      "criteria_recommended": [
        "heartbeat_age",
        "gpu_idle",
        "runtime_cap"
      ],
      "priority": "high (cycle 66 Pod 1 + Pod 2 모두 이 site에서 감지 가능했음)"
    },
    {
      "site": "tool/h100_stage2_unified_launch.bash",
      "role": "launch-side: pod-init.sh에 watchdog setup inject",
      "attach_point": "startup script append",
      "criteria_recommended": [
        "runtime_cap (env RUNTIME_CAP_MIN)",
        "cost_cap (env COST_CAP)"
      ],
      "priority": "high (env caps의 runtime attach gap — cycle 57 finding 직접 해결)"
    },
    {
      "site": "tool/h100_auto_kill.hexa",
      "role": "host-side kill (cycle 63 probe-exec gap path B)",
      "attach_point": "_evaluate_pod 내부 idle 측정 후 _stuck_by_gpu_idle_min 호출",
      "criteria_recommended": [
        "gpu_idle (probe 결과 활용)"
      ],
      "priority": "medium (path B 패치와 함께)"
    },
    {
      "site": "tool/h100_post_launch_ingest.hexa",
      "role": "ingest pipeline 중간 stuck (shard prefetch 멈춤)",
      "attach_point": "each shard barrier 후 heartbeat 검증",
      "criteria_recommended": [
        "heartbeat_age (per-shard mtime)"
      ],
      "priority": "low"
    }
  ],
  "evidence_tie_in": {
    "cycle_57_finding": "env caps set but no runtime attachment → fail open",
    "cycle_66_pod_2_class": "host scheduling failure (uptime=0, billing accrued)",
    "cycle_66_pod_1_class": "gpu_idle 100% with cap escape",
    "remediation_class": "pod-side self-watchdog 4-criterion OR composition",
    "cap_escape_prevention": "cost_cap_cents + runtime_cap 직접 enforced (no env-only)"
  },
  "residual_actions": [
    {
      "action": "cascade hexa에 inline integrate (cycle 68 후보)",
      "priority": "high",
      "cost": "mac $0",
      "requires": "사용자 승인"
    },
    {
      "action": "unified_launch.bash에 pod-init.sh watchdog inject",
      "priority": "high",
      "cost": "mac $0",
      "requires": "사용자 승인 + RUNPOD_API_KEY 정책"
    },
    {
      "action": "h100_auto_kill path B (probe-exec) + gpu_idle inline",
      "priority": "medium",
      "cost": "mac $0",
      "requires": "사용자 승인"
    },
    {
      "action": "real GPU validation (synthetic-only 한계)",
      "priority": "low",
      "cost": "$0.30 H100 P1 launch와 묶음",
      "requires": "사용자 GPU 승인"
    }
  ],
  "non_mutation_guarantee": "utility 신설 + landing 기록만; launch helper patch는 0건 (별도 cycle)",
  "cross_link_cycles": [
    "cycle 56 ssh activity",
    "cycle 57 cap enforcement gap",
    "cycle 63 probe-exec gap",
    "cycle 66 incident closure"
  ],
  "pattern": "governance audit chain layer 46 (utility 신설 — RunPod incident learning capture)"
}