{
  "schema": "anima.training_knob_spec.v1",
  "knob_id": "cpgd_active_fold",
  "roi_item": "B11",
  "roi_doc": "docs/h100_roi_improvements_20260422.md",
  "added": "2026-04-22",
  "owner_config": "config/alm_r13_v2_config.json",
  "owner_config_path": "training_knobs.cpgd_active_fold",
  "roadmap_dependencies": [26, 29],
  "roadmap_dep_status": "both done",
  "spec": {
    "wrapper": "edu/lora/cpgd_wrapper.hexa",
    "minimal_proof": "edu/lora/cpgd_minimal_proof.hexa",
    "fold_mode": "active (gradient projection onto cert manifold each step)",
    "passive_alternative": "off (wrapper present but bypassed) — current default"
  },
  "wiring": {
    "env_var": "ANIMA_CPGD_ACTIVE",
    "kickoff_flag": "--cpgd-active",
    "stage1_kickoff_command": "tool/h100_launch_manifest_spec.hexa Stage-1 kickoff_command (now includes both env var and flag)",
    "fallback_behavior": "wrapper passive (current default if env var unset and flag absent)"
  },
  "expected_signal": "cert manifold gradient → sample efficiency 2-3x; verified at #26+#29",
  "risk": "0 (already validated in formal proof check)",
  "rollback": "unset ANIMA_CPGD_ACTIVE and remove --cpgd-active from kickoff command"
}
