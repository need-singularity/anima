{
  "schema": "anima.pod_autoscale_spec.v1",
  "spec_status": "SPEC_ONLY_NOT_ACTIVE",
  "roi_item": "K59",
  "tool": "tool/pod_autoscale_spec.hexa",
  "created": "2026-04-22T11:19:40Z",
  "activation_gate": "post_CP2 + #59_launch_decision + actuator_tool_landed",
  "no_actual_runpodctl": true,
  "policy": {
    "scale_up_threshold": 10,
    "scale_down_threshold": 3,
    "max_pods": 8,
    "min_pods": 1,
    "cooldown_steps": 2,
    "initial_pods": 2,
    "signal_source_planned": "queue depth from state/job_queue.json (planned) OR runpodctl pod list + pending workitem count",
    "actuator_planned": "runpodctl pod create --gpuType H100SXM (up) / runpodctl pod terminate POD_ID (down) — DEFERRED, NOT IMPLEMENTED"
  },
  "smoke_trajectory": {
    "n_steps": 8,
    "depth_trace": [2, 15, 20, 18, 13, 8, 2, 1],
    "decisions": [
      {"step": 0, "depth": 2, "pre_pods": 2, "post_pods": 1, "decision": "scale_down"},
      {"step": 1, "depth": 15, "pre_pods": 1, "post_pods": 1, "decision": "noop"},
      {"step": 2, "depth": 20, "pre_pods": 1, "post_pods": 1, "decision": "noop"},
      {"step": 3, "depth": 18, "pre_pods": 1, "post_pods": 2, "decision": "scale_up"},
      {"step": 4, "depth": 13, "pre_pods": 2, "post_pods": 2, "decision": "noop"},
      {"step": 5, "depth": 8, "pre_pods": 2, "post_pods": 2, "decision": "noop"},
      {"step": 6, "depth": 2, "pre_pods": 2, "post_pods": 1, "decision": "scale_down"},
      {"step": 7, "depth": 1, "pre_pods": 1, "post_pods": 1, "decision": "noop"}
    ]
  },
  "counters": {
    "scale_up_events": 1,
    "scale_down_events": 2,
    "noop_events": 5,
    "max_pods_seen": 2,
    "min_pods_seen": 1,
    "bounds_violations": 0
  },
  "verdict": {
    "bounds_ok": true,
    "exercised_both_directions": true,
    "policy_self_consistent": true,
    "verdict": "SPEC_VERIFIED_NO_ACTUATOR"
  },
  "audit_notes": {
    "fake_pass_guard": "Pure synthetic trajectory; no runpodctl call, no pod created or destroyed.",
    "raw_strict": "raw#9 raw#11 raw#15 deterministic LLM=none"
  }
}
