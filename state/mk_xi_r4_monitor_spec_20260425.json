{
  "schema": "anima/mk_xi_r4_monitor_spec/1",
  "purpose": "r4-equivalent epoch trajectory monitor algorithm spec — V2 + V_pairrank dual-lever sweet spot detection + early-stop trigger. raw#12 frozen 2026-04-25. 도구 실 구현은 S5 forward cycle (training infra 필요).",
  "spec_doc": "docs/mk_xi_implementation_roadmap_20260425.md §4",
  "ts": "2026-04-25",
  "tool_name_planned": "tool/an11_b_r4_sweet_spot_monitor.hexa",
  "rationale": "round progression이 V2+V_pairrank 두 lever 모두 monotone decreasing (project_r4_sweet_spot_cross_base.md). r4-equivalent epoch에서 dual-lever 최적. over-training이 paired binding 망가뜨림.",
  "algorithm": {
    "input": "training trajectory checkpoints {c_s : s in train_steps}",
    "per_checkpoint": [
      "Step 1: H_c = forward(c, 16 prompts)",
      "Step 2: v2_papo_result = an11_b_v2_papo_multi_axis(H_c, k_max=5)  # exclude k=6 trivial",
      "Step 3: v2_pass_count = count of (k, alpha) with verdict=='PASS' and k <= 5",
      "Step 4: v_pairrank_result = an11_b_v_pairrank(H_c)",
      "Step 5: v_pairrank_top3 = v_pairrank_result.top3_hit",
      "Step 6: sweet_spot_signal = (v2_pass_count >= 1) AND (v_pairrank_top3 >= 0.10)"
    ],
    "trajectory_logic": [
      "When sweet_spot_signal first becomes TRUE: snapshot checkpoint c, mark r4_star_step = s",
      "When sweet_spot_signal degrades (FALSE) for >= N_lookback consecutive checkpoints: EARLY_STOP",
      "Restore r4_star_step checkpoint as final output"
    ]
  },
  "hyperparameters_frozen": {
    "N_lookback": 3,
    "monitor_interval_steps": "100-500 (heuristic, depends on r4 epoch position)",
    "v2_pass_count_threshold": 1,
    "v2_pass_k_max": 5,
    "v_pairrank_top3_threshold": 0.10,
    "rationale_for_thresholds": "r4 substrate proof: V2 PAPO non-trivial PASS 4 configs (k=2-5), V_pairrank top3=16.7%. 본 thresholds는 그 50-60% level — r4 reach 신호로 작동, full r4 미달도 catch."
  },
  "edge_cases": {
    "no_sweet_spot_during_training": {
      "scenario": "current LoRA처럼 sweet_spot_signal이 끝까지 FALSE — V2 anti-lever or V_pairrank degenerate",
      "action": "FAIL_TO_REACH_SWEET_SPOT verdict, all checkpoints saved for post-mortem"
    },
    "multiple_sweet_spots": {
      "scenario": "trajectory 내 복수 sweet spot (oscillation)",
      "action": "best (highest v2_pass_count + v_pairrank_top3) sweet spot 선택"
    },
    "false_positive_at_step_0": {
      "scenario": "BASE model이 우연히 threshold 만족",
      "action": "min_train_steps = 100 (frozen) — 그 전엔 sweet_spot 무시"
    }
  },
  "output_artifacts": {
    "trajectory_log": "state/mk_xi_r4_monitor_trajectory_<run_id>.jsonl  # per-checkpoint metrics",
    "sweet_spot_record": "state/mk_xi_r4_monitor_sweet_spot_<run_id>.json  # final r4_star_step + early_stop reason",
    "restored_checkpoint_path": "training_runs/<run_id>/checkpoints/step_<r4_star_step>/"
  },
  "raw10_honest_scope": [
    "thresholds heuristic from substrate finding — real fp 측정 후 raw#12 new revision 가능",
    "monitor_interval은 specific dataset/lr/batch에 의존 — r4 epoch 위치가 달라질 가능성",
    "V_pairrank top3 0.10 threshold가 너무 낮아 noise에 false positive 가능 — N_lookback=3로 stability 보강",
    "본 spec은 알고리즘만; 실 구현은 training infra (PyTorch/TF/JAX) wrapping 필요"
  ],
  "raw_compliance": {
    "raw_9_hexa_only_deterministic": "satisfied — algorithm spec only, $0",
    "raw_10_no_overclaim": "satisfied — heuristic thresholds 명시, infra dependency 명시, edge case false positive 명시",
    "raw_12_cherry_pick_proof": "satisfied — N_lookback, monitor_interval, thresholds 모두 frozen, edge cases 사전 등록",
    "raw_15_ssot": "this file = SSOT for r4 monitoring algorithm",
    "raw_37_38_omega_saturation_cycle": "design (round progression finding) → impl (this spec) → fixpoint"
  }
}
