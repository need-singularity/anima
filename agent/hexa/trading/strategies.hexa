// strategies.hexa — Expanded strategy library (30+ strategies)
// Port of agent/trading/strategies.py

// Categories:
//   Trend: MACD, EMA crossover, ADX, multi-timeframe
//   Mean reversion: RSI, Bollinger, VWAP, spread reversal
//   Momentum: time-series, acceleration, factor
//   Volatility: breakout, mean-revert, low-vol trend, Garman-Klass
//   Liquidity: volume surge, filter, spread reversal
//   Sentiment: fear/greed index, contrarian
//   SOC/Criticality: sandpile avalanche, power-law tail
//   Consciousness-native: PureField tension, Phi-momentum, cycles

fn get_all_strategies() {
    // TODO: return list of all available strategy instances
    print("get_all_strategies stub")
}

fn macd_strategy() { print("macd_strategy stub") }
fn rsi_strategy() { print("rsi_strategy stub") }
fn bollinger_strategy() { print("bollinger_strategy stub") }
fn ema_crossover_strategy() { print("ema_crossover_strategy stub") }
fn adx_trend_strategy() { print("adx_trend_strategy stub") }
fn vwap_reversion_strategy() { print("vwap_reversion_strategy stub") }
fn vol_breakout_strategy() { print("vol_breakout_strategy stub") }
fn soc_avalanche_strategy() { print("soc_avalanche_strategy stub") }
fn phi_momentum_strategy() { print("phi_momentum_strategy stub") }
fn purefield_tension_strategy() { print("purefield_tension_strategy stub") }

fn main() {
    print("trading/strategies.hexa — TODO: port 30+ strategies from strategies.py")
}
