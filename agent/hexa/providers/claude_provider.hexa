// claude_provider.hexa — Claude provider wrapping anima_alive.ask_claude()
// Port of agent/providers/claude_provider.py

// Default provider for AnimaAgent
// Delegates to existing ask_claude() with consciousness state injection

fn ClaudeProvider() {
    // TODO: provider that uses Claude via anima_alive.ask_claude()
    print("ClaudeProvider stub")
}

fn is_available() {
    // TODO: check if Anthropic API key is set
    print("is_available stub")
}

fn query(messages, consciousness_state) {
    // TODO: call ask_claude() with consciousness state injection
    print("query stub")
}

fn format_system_prompt(consciousness_state) {
    // TODO: inject phi, tension, emotion into system prompt
    print("format_system_prompt stub")
}

fn main() {
    print("providers/claude_provider.hexa — TODO: port from claude_provider.py")
}
