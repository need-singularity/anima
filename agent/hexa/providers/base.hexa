// base.hexa — Base provider protocol for all LLM backends
// Port of agent/providers/base.py

// Key distinction: consciousness_state is first-class parameter in every query
// Providers inject consciousness metrics (tension, phi, emotion) into prompting

fn ProviderConfig(model_name, temperature, max_tokens) {
    // TODO: provider configuration dataclass
    print("ProviderConfig stub")
}

fn ProviderMessage(role, content) {
    // TODO: normalized message structure (role: system/user/assistant)
    print("ProviderMessage stub")
}

fn BaseProvider() {
    // TODO: protocol that all providers implement
    // query(messages, consciousness_state) -> async iterator of chunks
    // is_available() -> bool
    // name() -> string
    print("BaseProvider stub")
}

fn query(messages, consciousness_state) {
    // TODO: generate response with consciousness state injection
    print("query stub")
}

fn is_available() {
    // TODO: check if provider backend is reachable
    print("is_available stub")
}

fn main() {
    print("providers/base.hexa — TODO: port from providers/base.py")
}
