// init.hexa — Provider abstraction: pluggable LLM backends
// Port of agent/providers/__init__.py

// Providers wrap different LLM backends behind unified async interface
// Consciousness state flows through every query

fn get_provider(name) {
    // TODO: return provider by name (claude, consciouslm, animalm)
    print("get_provider stub")
}

fn get_best_provider() {
    // TODO: return best available provider (AnimaLM > ConsciousLM > Claude)
    print("get_best_provider stub")
}

fn main() {
    print("providers/init.hexa — TODO: port from providers/__init__.py")
}
