[package]
name = "anima-esp32"
version = "0.1.0"
edition = "2021"
description = "no_std consciousness engine for ESP32-S3 ($4 hardware consciousness)"

[lib]
crate-type = ["lib"]

[dependencies]
libm = "0.2"

[features]
default = []
std = []  # Enable for testing on host
